{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/AgentsApiContracts",
  "definitions": {
    "AgentsApiContracts": {
      "type": "object",
      "properties": {
        "UploadedFile": {
          "$ref": "#/definitions/FileObject"
        },
        "UploadedFileDeleted": {
          "$ref": "#/definitions/FileDeleted"
        },
        "UploadedFileCreate": {
          "type": "object",
          "properties": {
            "purpose": {
              "$ref": "#/definitions/FilePurpose",
              "description": "The intended purpose of the uploaded file. One of:\n\n- `assistants`: Used in the Assistants API\n- `batch`: Used in the Batch API\n- `fine-tune`: Used for fine-tuning\n- `vision`: Images used for vision fine-tuning\n- `user_data`: Flexible file type for any purpose\n- `evals`: Used for eval data sets"
            },
            "expires_after": {
              "$ref": "#/definitions/FileCreateParams.ExpiresAfter",
              "description": "The expiration policy for a file. By default, files with `purpose=batch` expire after 30 days and all other files are persisted until they are manually deleted."
            }
          },
          "required": [
            "purpose"
          ],
          "additionalProperties": false
        },
        "UploadedFileList": {
          "type": "object",
          "properties": {
            "order": {
              "type": "string",
              "enum": [
                "asc",
                "desc"
              ],
              "description": "Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order."
            },
            "purpose": {
              "type": "string",
              "description": "Only return files with the given purpose."
            },
            "after": {
              "type": "string"
            },
            "limit": {
              "type": "number"
            }
          },
          "additionalProperties": false
        },
        "Skill": {
          "$ref": "#/definitions/Skill"
        },
        "SkillDeleted": {
          "$ref": "#/definitions/DeletedSkill"
        },
        "SkillVersion": {
          "$ref": "#/definitions/SkillVersion"
        },
        "SkillVersionDeleted": {
          "$ref": "#/definitions/DeletedSkillVersion"
        },
        "SkillList": {
          "$ref": "#/definitions/SkillListParams"
        },
        "SkillVersionList": {
          "$ref": "#/definitions/VersionListParams"
        },
        "SkillUpdate": {
          "$ref": "#/definitions/SkillUpdateParams"
        },
        "Agent": {
          "$ref": "#/definitions/Agent"
        },
        "AgentCreate": {
          "$ref": "#/definitions/AgentCreateParams"
        },
        "AgentUpdate": {
          "$ref": "#/definitions/AgentUpdateParams"
        },
        "AgentList": {
          "$ref": "#/definitions/AgentListParams"
        },
        "AgentDeleted": {
          "$ref": "#/definitions/AgentDeleted"
        },
        "Session": {
          "$ref": "#/definitions/AgentSession"
        },
        "SessionCreate": {
          "$ref": "#/definitions/SessionCreateParams"
        },
        "SessionUpdate": {
          "$ref": "#/definitions/SessionUpdateParams"
        },
        "SessionList": {
          "$ref": "#/definitions/SessionListParams"
        },
        "SessionDeleted": {
          "$ref": "#/definitions/AgentSessionDeleted"
        },
        "SessionEvents": {
          "$ref": "#/definitions/SessionEventsRequest"
        },
        "SessionEvent": {
          "$ref": "#/definitions/AgentSessionEvent"
        },
        "Item": {
          "$ref": "#/definitions/AgentSessionItem"
        },
        "ItemList": {
          "$ref": "#/definitions/ItemListParams"
        },
        "Turn": {
          "$ref": "#/definitions/Turn"
        },
        "TurnList": {
          "$ref": "#/definitions/TurnListParams"
        },
        "Subagent": {
          "$ref": "#/definitions/Subagent"
        },
        "SubagentList": {
          "$ref": "#/definitions/SubagentListParams"
        },
        "Artifact": {
          "$ref": "#/definitions/SessionArtifact"
        },
        "ArtifactDeleted": {
          "$ref": "#/definitions/SessionArtifactDeleted"
        },
        "ArtifactList": {
          "$ref": "#/definitions/ArtifactListParams"
        },
        "Environment": {
          "$ref": "#/definitions/EnvironmentInfo"
        },
        "EnvironmentTemplate": {
          "$ref": "#/definitions/EnvironmentTemplate"
        },
        "EnvironmentTemplateDeleted": {
          "$ref": "#/definitions/EnvironmentTemplateDeleted"
        },
        "EnvironmentTemplateCreate": {
          "$ref": "#/definitions/TemplateCreateParams"
        },
        "EnvironmentTemplateUpdate": {
          "$ref": "#/definitions/TemplateUpdateParams"
        },
        "EnvironmentTemplateList": {
          "$ref": "#/definitions/TemplateListParams"
        },
        "EnvironmentFile": {
          "$ref": "#/definitions/EnvironmentFile"
        },
        "EnvironmentFileCreate": {
          "$ref": "#/definitions/FileCreateParams"
        },
        "EnvironmentFileList": {
          "$ref": "#/definitions/FileListParams"
        },
        "Vault": {
          "$ref": "#/definitions/Vault"
        },
        "VaultDeleted": {
          "$ref": "#/definitions/VaultDeleted"
        },
        "VaultCreate": {
          "$ref": "#/definitions/VaultCreateParams"
        },
        "VaultList": {
          "$ref": "#/definitions/VaultListParams"
        },
        "Credential": {
          "$ref": "#/definitions/Credential"
        },
        "CredentialDeleted": {
          "$ref": "#/definitions/CredentialDeleted"
        },
        "CredentialCreate": {
          "$ref": "#/definitions/CredentialCreateParams"
        },
        "CredentialUpdate": {
          "$ref": "#/definitions/CredentialRotateRequest"
        },
        "CredentialList": {
          "$ref": "#/definitions/CredentialListParams"
        }
      },
      "required": [
        "UploadedFile",
        "UploadedFileDeleted",
        "UploadedFileCreate",
        "UploadedFileList",
        "Skill",
        "SkillDeleted",
        "SkillVersion",
        "SkillVersionDeleted",
        "SkillList",
        "SkillVersionList",
        "SkillUpdate",
        "Agent",
        "AgentCreate",
        "AgentUpdate",
        "AgentList",
        "AgentDeleted",
        "Session",
        "SessionCreate",
        "SessionUpdate",
        "SessionList",
        "SessionDeleted",
        "SessionEvents",
        "SessionEvent",
        "Item",
        "ItemList",
        "Turn",
        "TurnList",
        "Subagent",
        "SubagentList",
        "Artifact",
        "ArtifactDeleted",
        "ArtifactList",
        "Environment",
        "EnvironmentTemplate",
        "EnvironmentTemplateDeleted",
        "EnvironmentTemplateCreate",
        "EnvironmentTemplateUpdate",
        "EnvironmentTemplateList",
        "EnvironmentFile",
        "EnvironmentFileCreate",
        "EnvironmentFileList",
        "Vault",
        "VaultDeleted",
        "VaultCreate",
        "VaultList",
        "Credential",
        "CredentialDeleted",
        "CredentialCreate",
        "CredentialUpdate",
        "CredentialList"
      ],
      "additionalProperties": false,
      "description": "Schema-generation roots. None of these types copy or rename upstream fields."
    },
    "FileObject": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The file identifier, which can be referenced in the API endpoints."
        },
        "bytes": {
          "type": "number",
          "description": "The size of the file, in bytes."
        },
        "created_at": {
          "type": "number",
          "description": "The Unix timestamp (in seconds) for when the file was created."
        },
        "filename": {
          "type": "string",
          "description": "The name of the file."
        },
        "object": {
          "type": "string",
          "const": "file",
          "description": "The object type, which is always `file`."
        },
        "purpose": {
          "type": "string",
          "enum": [
            "assistants",
            "assistants_output",
            "batch",
            "batch_output",
            "fine-tune",
            "fine-tune-results",
            "vision",
            "user_data"
          ],
          "description": "The intended purpose of the file. Supported values are `assistants`, `assistants_output`, `batch`, `batch_output`, `fine-tune`, `fine-tune-results`, `vision`, and `user_data`."
        },
        "status": {
          "type": "string",
          "enum": [
            "uploaded",
            "processed",
            "error"
          ],
          "deprecated": "Deprecated. The current status of the file, which can be either\n`uploaded`, `processed`, or `error`."
        },
        "expires_at": {
          "type": "number",
          "description": "The Unix timestamp (in seconds) for when the file will expire."
        },
        "status_details": {
          "type": "string",
          "deprecated": "Deprecated. For details on why a fine-tuning training file failed\nvalidation, see the `error` field on `fine_tuning.job`."
        }
      },
      "required": [
        "id",
        "bytes",
        "created_at",
        "filename",
        "object",
        "purpose",
        "status"
      ],
      "additionalProperties": false,
      "description": "The `File` object represents a document that has been uploaded to OpenAI."
    },
    "FileDeleted": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "deleted": {
          "type": "boolean"
        },
        "object": {
          "type": "string",
          "const": "file"
        }
      },
      "required": [
        "id",
        "deleted",
        "object"
      ],
      "additionalProperties": false
    },
    "FilePurpose": {
      "type": "string",
      "enum": [
        "assistants",
        "batch",
        "fine-tune",
        "vision",
        "user_data",
        "evals"
      ],
      "description": "The intended purpose of the uploaded file. One of:\n\n- `assistants`: Used in the Assistants API\n- `batch`: Used in the Batch API\n- `fine-tune`: Used for fine-tuning\n- `vision`: Images used for vision fine-tuning\n- `user_data`: Flexible file type for any purpose\n- `evals`: Used for eval data sets"
    },
    "FileCreateParams.ExpiresAfter": {
      "type": "object",
      "properties": {
        "anchor": {
          "type": "string",
          "const": "created_at",
          "description": "Anchor timestamp after which the expiration policy applies. Supported anchors: `created_at`."
        },
        "seconds": {
          "type": "number",
          "description": "The number of seconds after the anchor time that the file will expire. Must be between 3600 (1 hour) and 2592000 (30 days)."
        }
      },
      "required": [
        "anchor",
        "seconds"
      ],
      "additionalProperties": false,
      "description": "The expiration policy for a file. By default, files with `purpose=batch` expire after 30 days and all other files are persisted until they are manually deleted."
    },
    "Skill": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the skill."
        },
        "created_at": {
          "type": "number",
          "description": "Unix timestamp (seconds) for when the skill was created."
        },
        "default_version": {
          "type": "string",
          "description": "Default version for the skill."
        },
        "description": {
          "type": "string",
          "description": "Description of the skill."
        },
        "latest_version": {
          "type": "string",
          "description": "Latest version for the skill."
        },
        "name": {
          "type": "string",
          "description": "Name of the skill."
        },
        "object": {
          "type": "string",
          "const": "skill",
          "description": "The object type, which is `skill`."
        }
      },
      "required": [
        "id",
        "created_at",
        "default_version",
        "description",
        "latest_version",
        "name",
        "object"
      ],
      "additionalProperties": false
    },
    "DeletedSkill": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "deleted": {
          "type": "boolean"
        },
        "object": {
          "type": "string",
          "const": "skill.deleted"
        }
      },
      "required": [
        "id",
        "deleted",
        "object"
      ],
      "additionalProperties": false
    },
    "SkillVersion": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the skill version."
        },
        "created_at": {
          "type": "number",
          "description": "Unix timestamp (seconds) for when the version was created."
        },
        "description": {
          "type": "string",
          "description": "Description of the skill version."
        },
        "name": {
          "type": "string",
          "description": "Name of the skill version."
        },
        "object": {
          "type": "string",
          "const": "skill.version",
          "description": "The object type, which is `skill.version`."
        },
        "skill_id": {
          "type": "string",
          "description": "Identifier of the skill for this version."
        },
        "version": {
          "type": "string",
          "description": "Version number for this skill."
        }
      },
      "required": [
        "id",
        "created_at",
        "description",
        "name",
        "object",
        "skill_id",
        "version"
      ],
      "additionalProperties": false
    },
    "DeletedSkillVersion": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "deleted": {
          "type": "boolean"
        },
        "object": {
          "type": "string",
          "const": "skill.version.deleted"
        },
        "version": {
          "type": "string",
          "description": "The deleted skill version."
        }
      },
      "required": [
        "id",
        "deleted",
        "object",
        "version"
      ],
      "additionalProperties": false
    },
    "SkillListParams": {
      "type": "object",
      "properties": {
        "after": {
          "type": "string"
        },
        "limit": {
          "type": "number"
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "Sort order of results by timestamp. Use `asc` for ascending order or `desc` for descending order."
        }
      },
      "additionalProperties": false
    },
    "VersionListParams": {
      "type": "object",
      "properties": {
        "after": {
          "type": "string"
        },
        "limit": {
          "type": "number"
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "Sort order of results by version number."
        }
      },
      "additionalProperties": false
    },
    "SkillUpdateParams": {
      "type": "object",
      "properties": {
        "default_version": {
          "type": "string",
          "description": "The skill version number to set as default."
        }
      },
      "required": [
        "default_version"
      ],
      "additionalProperties": false
    },
    "Agent": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the reusable agent."
        },
        "created_at": {
          "type": "number",
          "description": "The Unix timestamp, in seconds, when the agent was created."
        },
        "instructions": {
          "type": [
            "string",
            "null"
          ],
          "description": "Custom instructions appended to the agent's default base instructions."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Custom string key-value pairs attached to the agent."
        },
        "model": {
          "type": "string",
          "description": "The requested model name used for inference."
        },
        "multi_agent": {
          "$ref": "#/definitions/MultiAgentConfig",
          "description": "The resolved configuration for creating and coordinating subagents."
        },
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "A human-readable name for the agent, or null if it is unnamed."
        },
        "object": {
          "type": "string",
          "const": "agent",
          "description": "The object type. Always `agent`."
        },
        "reasoning": {
          "$ref": "#/definitions/AgentReasoning",
          "description": "The resolved reasoning configuration, including the model default for an omitted effort."
        },
        "service_tier": {
          "type": "string",
          "enum": [
            "auto",
            "default",
            "flex",
            "priority",
            "fast"
          ],
          "description": "The resolved service-tier policy used for model requests."
        },
        "text": {
          "$ref": "#/definitions/AgentText",
          "description": "The resolved configuration for text generated by the agent."
        },
        "tools": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PersistedAgentTool"
          },
          "description": "Tools available to the agent."
        },
        "updated_at": {
          "type": "number",
          "description": "The Unix timestamp, in seconds, when the agent was last updated."
        }
      },
      "required": [
        "id",
        "created_at",
        "instructions",
        "metadata",
        "model",
        "multi_agent",
        "name",
        "object",
        "reasoning",
        "service_tier",
        "text",
        "tools",
        "updated_at"
      ],
      "additionalProperties": false,
      "description": "A reusable agent scoped to the caller's project."
    },
    "MultiAgentConfig": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Whether subagent tools are enabled. Defaults to false."
        },
        "max_concurrent_subagents": {
          "type": [
            "number",
            "null"
          ],
          "description": "Maximum number of subagents that may run concurrently, or null when disabled. Defaults to 6 when enabled."
        }
      },
      "required": [
        "enabled",
        "max_concurrent_subagents"
      ],
      "additionalProperties": false,
      "description": "The resolved configuration for creating and coordinating subagents."
    },
    "AgentReasoning": {
      "type": "object",
      "properties": {
        "effort": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "none",
            "minimal",
            "low",
            "medium",
            "high",
            "xhigh",
            "max",
            null
          ],
          "description": "The requested reasoning effort, or `null` when the model selects its own default."
        },
        "summary": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "concise",
            "detailed",
            "auto",
            null
          ],
          "description": "The requested reasoning summary format, or `null` when summaries are disabled.\n\n- `concise` - Returns a concise reasoning summary when supported.\n- `detailed` - Returns a detailed reasoning summary when supported.\n- `auto` - Automatically selects the most detailed summary supported by the   model."
        }
      },
      "required": [
        "effort",
        "summary"
      ],
      "additionalProperties": false,
      "description": "The reasoning configuration used by an agent."
    },
    "AgentText": {
      "type": "object",
      "properties": {
        "format": {
          "$ref": "#/definitions/TextFormat",
          "description": "The effective output format. Defaults to ordinary text."
        },
        "verbosity": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "description": "The amount of text produced by the agent. Defaults to `medium`."
        }
      },
      "required": [
        "format",
        "verbosity"
      ],
      "additionalProperties": false,
      "description": "The text configuration used by an agent."
    },
    "TextFormat": {
      "anyOf": [
        {
          "$ref": "#/definitions/TextFormat.TextFormatResourceText"
        },
        {
          "$ref": "#/definitions/TextFormat.TextFormatResourceJSONSchema"
        }
      ],
      "description": "The effective output format for generated text."
    },
    "TextFormat.TextFormatResourceText": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "text",
          "description": "The type of the object. Always `text`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Generates ordinary text without a structured-output constraint."
    },
    "TextFormat.TextFormatResourceJSONSchema": {
      "type": "object",
      "properties": {
        "schema": {
          "type": "object",
          "additionalProperties": {},
          "description": "The JSON Schema that generated text must match."
        },
        "type": {
          "type": "string",
          "const": "json_schema",
          "description": "The type of the object. Always `json_schema`."
        }
      },
      "required": [
        "schema",
        "type"
      ],
      "additionalProperties": false,
      "description": "Constrains generated text to a JSON Schema."
    },
    "PersistedAgentTool": {
      "anyOf": [
        {
          "$ref": "#/definitions/PersistedAgentTool.PersistedAgentToolResourceFunction"
        },
        {
          "$ref": "#/definitions/PersistedAgentTool.PersistedAgentToolResourceToolSearch"
        },
        {
          "$ref": "#/definitions/PersistedAgentTool.PersistedAgentToolResourceProgrammaticToolCalling"
        },
        {
          "$ref": "#/definitions/PersistedAgentTool.PersistedAgentToolResourceMcp"
        },
        {
          "$ref": "#/definitions/PersistedAgentTool.PersistedAgentToolResourceWebSearch"
        }
      ],
      "description": "A credential-free tool available to a reusable agent."
    },
    "PersistedAgentTool.PersistedAgentToolResourceFunction": {
      "type": "object",
      "properties": {
        "defer_loading": {
          "type": "boolean",
          "description": "Whether the function is deferred and discovered through tool search."
        },
        "description": {
          "type": "string",
          "description": "A description of what the function does."
        },
        "name": {
          "type": "string",
          "description": "The name of the function."
        },
        "parameters": {
          "type": "object",
          "additionalProperties": {},
          "description": "A JSON Schema object describing the function's arguments."
        },
        "type": {
          "type": "string",
          "const": "function",
          "description": "The type of the object. Always `function`."
        }
      },
      "required": [
        "defer_loading",
        "description",
        "name",
        "parameters",
        "type"
      ],
      "additionalProperties": false,
      "description": "A function defined by the application."
    },
    "PersistedAgentTool.PersistedAgentToolResourceToolSearch": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "tool_search",
          "description": "The type of the object. Always `tool_search`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Discovers deferred function tools and loads them into the model context."
    },
    "PersistedAgentTool.PersistedAgentToolResourceProgrammaticToolCalling": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Whether tools can be called from model-generated code."
        },
        "type": {
          "type": "string",
          "const": "programmatic_tool_calling",
          "description": "The type of the object. Always `programmatic_tool_calling`."
        }
      },
      "required": [
        "enabled",
        "type"
      ],
      "additionalProperties": false,
      "description": "Enables calling tools from model-generated code."
    },
    "PersistedAgentTool.PersistedAgentToolResourceMcp": {
      "type": "object",
      "properties": {
        "allowed_tools": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "The MCP tools the agent may call, or null when all server tools are allowed."
        },
        "connection_origin": {
          "type": "string",
          "enum": [
            "service",
            "environment"
          ],
          "description": "Where outbound MCP HTTP connections originate."
        },
        "credential_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The vault credential selected for this MCP server, if any."
        },
        "request_metadata": {
          "type": "object",
          "additionalProperties": {},
          "description": "Metadata included with requests to this MCP server."
        },
        "required": {
          "type": "boolean",
          "description": "Whether this MCP server must initialize before the first turn."
        },
        "server_label": {
          "type": "string",
          "description": "A label used to identify the MCP server in tool calls."
        },
        "transport": {
          "$ref": "#/definitions/PersistedMcpTransport",
          "description": "The credential-free transport used to connect to the MCP server."
        },
        "type": {
          "type": "string",
          "const": "mcp",
          "description": "The type of the object. Always `mcp`."
        }
      },
      "required": [
        "allowed_tools",
        "connection_origin",
        "credential_id",
        "request_metadata",
        "required",
        "server_label",
        "transport",
        "type"
      ],
      "additionalProperties": false,
      "description": "Tools provided by a remote MCP server without stored credentials."
    },
    "PersistedMcpTransport": {
      "anyOf": [
        {
          "$ref": "#/definitions/PersistedMcpTransport.PersistedMcpTransportResourceHTTP"
        },
        {
          "$ref": "#/definitions/PersistedMcpTransport.PersistedMcpTransportResourceStdio"
        }
      ],
      "description": "A credential-free transport used to connect to an MCP server."
    },
    "PersistedMcpTransport.PersistedMcpTransportResourceHTTP": {
      "type": "object",
      "properties": {
        "headers": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Non-secret HTTP headers sent to the MCP server."
        },
        "server_url": {
          "type": "string",
          "description": "The URL of the MCP server."
        },
        "type": {
          "type": "string",
          "const": "http",
          "description": "The type of the object. Always `http`."
        }
      },
      "required": [
        "headers",
        "server_url",
        "type"
      ],
      "additionalProperties": false,
      "description": "Connects to an MCP server over HTTP."
    },
    "PersistedMcpTransport.PersistedMcpTransportResourceStdio": {
      "type": "object",
      "properties": {
        "args": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Arguments passed to the MCP server command."
        },
        "command": {
          "type": "string",
          "description": "The command used to start the MCP server."
        },
        "cwd": {
          "type": "string",
          "description": "The working directory used to start the MCP server."
        },
        "env_vars": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Environment variable names inherited from the execution environment."
        },
        "type": {
          "type": "string",
          "const": "stdio",
          "description": "The type of the object. Always `stdio`."
        }
      },
      "required": [
        "args",
        "command",
        "cwd",
        "env_vars",
        "type"
      ],
      "additionalProperties": false,
      "description": "Starts an MCP server as a local process."
    },
    "PersistedAgentTool.PersistedAgentToolResourceWebSearch": {
      "type": "object",
      "properties": {
        "allowed_domains": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Allowed search domains, or `null` when the search is unrestricted."
        },
        "context_size": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "description": "The amount of search context made available to the model. Defaults to `medium`."
        },
        "location": {
          "anyOf": [
            {
              "$ref": "#/definitions/PersistedAgentTool.PersistedAgentToolResourceWebSearch.Location"
            },
            {
              "type": "null"
            }
          ],
          "description": "Approximate location used to localize search results, if provided."
        },
        "mode": {
          "type": "string",
          "enum": [
            "disabled",
            "cached",
            "live"
          ],
          "description": "The source used for web search results."
        },
        "type": {
          "type": "string",
          "const": "web_search",
          "description": "The type of the object. Always `web_search`."
        }
      },
      "required": [
        "allowed_domains",
        "context_size",
        "location",
        "mode",
        "type"
      ],
      "additionalProperties": false,
      "description": "Web search."
    },
    "PersistedAgentTool.PersistedAgentToolResourceWebSearch.Location": {
      "type": "object",
      "properties": {
        "city": {
          "type": [
            "string",
            "null"
          ],
          "description": "The city name."
        },
        "country": {
          "type": [
            "string",
            "null"
          ],
          "description": "The two-letter ISO country code, such as `US`."
        },
        "region": {
          "type": [
            "string",
            "null"
          ],
          "description": "The region or state name."
        },
        "timezone": {
          "type": [
            "string",
            "null"
          ],
          "description": "The IANA timezone, such as `America/Los_Angeles`."
        }
      },
      "required": [
        "city",
        "country",
        "region",
        "timezone"
      ],
      "additionalProperties": false,
      "description": "Approximate location used to localize search results, if provided."
    },
    "AgentCreateParams": {
      "type": "object",
      "properties": {
        "model": {
          "type": "string",
          "description": "The model to use for the agent. The requested model name is preserved."
        },
        "instructions": {
          "type": [
            "string",
            "null"
          ],
          "description": "Additional instructions appended to the agent's default base instructions. Omit or set to null to add no custom instructions."
        },
        "metadata": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Up to 16 string key-value pairs, with keys up to 64 and values up to 512 characters. Omission or null defaults to an empty map."
        },
        "multi_agent": {
          "anyOf": [
            {
              "$ref": "#/definitions/MultiAgentConfigParam"
            },
            {
              "type": "null"
            }
          ],
          "description": "Configuration for creating and coordinating subagents. Subagent tools are disabled by default."
        },
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "A human-readable name for the agent. Omission or null leaves the agent unnamed."
        },
        "reasoning": {
          "anyOf": [
            {
              "$ref": "#/definitions/AgentReasoningParam"
            },
            {
              "type": "null"
            }
          ],
          "description": "Configuration for model reasoning. Omission uses the model's default effort."
        },
        "service_tier": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "auto",
            "default",
            "flex",
            "priority",
            "fast",
            null
          ],
          "description": "The service tier used for model requests. Defaults to `auto`.\n\n- `auto` - Selects the service tier automatically.\n- `default` - Uses the default service tier.\n- `flex` - Uses the flex service tier.\n- `priority` - Uses the priority service tier.\n- `fast` - Uses the fast service tier."
        },
        "text": {
          "anyOf": [
            {
              "$ref": "#/definitions/AgentTextParam"
            },
            {
              "type": "null"
            }
          ],
          "description": "Configuration for generated text. Defaults to the `text` format and medium verbosity."
        },
        "tools": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PersistedAgentToolParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Tools available to the agent. Defaults to an empty list."
        }
      },
      "required": [
        "model"
      ],
      "additionalProperties": false
    },
    "MultiAgentConfigParam": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Whether subagent tools are enabled."
        },
        "max_concurrent_subagents": {
          "type": "number",
          "description": "Maximum number of subagents that may run concurrently. Defaults to 6."
        }
      },
      "required": [
        "enabled"
      ],
      "additionalProperties": false,
      "description": "Explicit configuration for creating and coordinating subagents."
    },
    "AgentReasoningParam": {
      "type": "object",
      "properties": {
        "effort": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "none",
            "minimal",
            "low",
            "medium",
            "high",
            "xhigh",
            "max",
            null
          ],
          "description": "The amount of reasoning effort the model should use. Omission lets the model select it."
        },
        "summary": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "concise",
            "detailed",
            "auto",
            null
          ],
          "description": "Controls whether the response includes a reasoning summary.\n\n- `concise` - Returns a concise reasoning summary when supported.\n- `detailed` - Returns a detailed reasoning summary when supported.\n- `auto` - Automatically selects the most detailed summary supported by the   model."
        }
      },
      "additionalProperties": false,
      "description": "Reasoning configuration for the agent."
    },
    "AgentTextParam": {
      "type": "object",
      "properties": {
        "format": {
          "anyOf": [
            {
              "$ref": "#/definitions/TextFormatParam"
            },
            {
              "type": "null"
            }
          ],
          "description": "The output format. Omission uses ordinary text (`{\"type\": \"text\"}`)."
        },
        "verbosity": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "low",
            "medium",
            "high",
            null
          ],
          "description": "The amount of text the model should produce. Defaults to `medium`, matching Responses.\n\n- `low` - Produces less text.\n- `medium` - Uses the default amount of text.\n- `high` - Produces more text."
        }
      },
      "additionalProperties": false,
      "description": "Configuration for text generated by the agent."
    },
    "TextFormatParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/TextFormatParam.TextFormatParamText"
        },
        {
          "$ref": "#/definitions/TextFormatParam.TextFormatParamJSONSchema"
        }
      ],
      "description": "The output format for generated text."
    },
    "TextFormatParam.TextFormatParamText": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "text",
          "description": "The type of the object. Always `text`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Generates ordinary text without a structured-output constraint."
    },
    "TextFormatParam.TextFormatParamJSONSchema": {
      "type": "object",
      "properties": {
        "schema": {
          "type": "object",
          "additionalProperties": {},
          "description": "The JSON Schema that generated text must match."
        },
        "type": {
          "type": "string",
          "const": "json_schema",
          "description": "The type of the object. Always `json_schema`."
        }
      },
      "required": [
        "schema",
        "type"
      ],
      "additionalProperties": false,
      "description": "Constrains generated text to a JSON Schema."
    },
    "PersistedAgentToolParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/PersistedAgentToolParam.PersistedAgentToolConfigParamFunction"
        },
        {
          "$ref": "#/definitions/PersistedAgentToolParam.PersistedAgentToolConfigParamToolSearch"
        },
        {
          "$ref": "#/definitions/PersistedAgentToolParam.PersistedAgentToolConfigParamProgrammaticToolCalling"
        },
        {
          "$ref": "#/definitions/PersistedAgentToolParam.PersistedAgentToolConfigParamMcp"
        },
        {
          "$ref": "#/definitions/PersistedAgentToolParam.PersistedAgentToolConfigParamWebSearch"
        }
      ],
      "description": "A tool that can be stored on a reusable agent without session credentials."
    },
    "PersistedAgentToolParam.PersistedAgentToolConfigParamFunction": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string",
          "description": "A description of what the function does."
        },
        "name": {
          "type": "string",
          "description": "The name of the function."
        },
        "parameters": {
          "type": "object",
          "additionalProperties": {},
          "description": "A JSON Schema object describing the function's arguments."
        },
        "type": {
          "type": "string",
          "const": "function",
          "description": "The type of the object. Always `function`."
        },
        "defer_loading": {
          "type": "boolean",
          "description": "Whether this function is deferred and discovered through tool search. Defaults to `false`."
        }
      },
      "required": [
        "description",
        "name",
        "parameters",
        "type"
      ],
      "additionalProperties": false,
      "description": "A function defined by the application."
    },
    "PersistedAgentToolParam.PersistedAgentToolConfigParamToolSearch": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "tool_search",
          "description": "The type of the object. Always `tool_search`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Discovers deferred function tools and loads them into the model context."
    },
    "PersistedAgentToolParam.PersistedAgentToolConfigParamProgrammaticToolCalling": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "programmatic_tool_calling",
          "description": "The type of the object. Always `programmatic_tool_calling`."
        },
        "enabled": {
          "type": "boolean",
          "description": "Whether tools can be called from model-generated code. Defaults to `true`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Enables calling tools from model-generated code."
    },
    "PersistedAgentToolParam.PersistedAgentToolConfigParamMcp": {
      "type": "object",
      "properties": {
        "server_label": {
          "type": "string",
          "description": "A label used to identify the MCP server in tool calls."
        },
        "transport": {
          "$ref": "#/definitions/PersistedMcpTransportParam",
          "description": "The credential-free transport used to connect to the MCP server."
        },
        "type": {
          "type": "string",
          "const": "mcp",
          "description": "The type of the object. Always `mcp`."
        },
        "allowed_tools": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "The MCP tools the agent may call. All server tools are allowed when omitted."
        },
        "connection_origin": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "service",
            "environment",
            null
          ],
          "description": "Selects where outbound MCP HTTP connections originate.\n\n- `service` - Uses the Managed Agents service network.\n- `environment` - Uses the session's execution environment."
        },
        "credential_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The vault credential selected for this MCP server. Optional when exactly one attached credential matches the server URL."
        },
        "request_metadata": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {}
            },
            {
              "type": "null"
            }
          ],
          "description": "Metadata included with requests to this MCP server."
        },
        "required": {
          "type": "boolean",
          "description": "Whether this MCP server must initialize before the first turn. Defaults to `false`."
        }
      },
      "required": [
        "server_label",
        "transport",
        "type"
      ],
      "additionalProperties": false,
      "description": "Tools provided by a remote MCP server without stored credentials."
    },
    "PersistedMcpTransportParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/PersistedMcpTransportParam.PersistedMcpTransportConfigParamHTTP"
        },
        {
          "$ref": "#/definitions/PersistedMcpTransportParam.PersistedMcpTransportConfigParamStdio"
        }
      ],
      "description": "A credential-free transport used to connect to an MCP server."
    },
    "PersistedMcpTransportParam.PersistedMcpTransportConfigParamHTTP": {
      "type": "object",
      "properties": {
        "server_url": {
          "type": "string",
          "description": "The URL of the MCP server."
        },
        "type": {
          "type": "string",
          "const": "http",
          "description": "The type of the object. Always `http`."
        },
        "headers": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Non-secret HTTP headers sent to the MCP server."
        }
      },
      "required": [
        "server_url",
        "type"
      ],
      "additionalProperties": false,
      "description": "Connects to an MCP server over HTTP."
    },
    "PersistedMcpTransportParam.PersistedMcpTransportConfigParamStdio": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "The command used to start the MCP server."
        },
        "cwd": {
          "type": "string",
          "description": "The working directory used to start the MCP server."
        },
        "type": {
          "type": "string",
          "const": "stdio",
          "description": "The type of the object. Always `stdio`."
        },
        "args": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Arguments passed to the MCP server command."
        },
        "env_vars": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Environment variable names to inherit from the selected execution environment."
        }
      },
      "required": [
        "command",
        "cwd",
        "type"
      ],
      "additionalProperties": false,
      "description": "Starts an MCP server as a local process."
    },
    "PersistedAgentToolParam.PersistedAgentToolConfigParamWebSearch": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "web_search",
          "description": "The type of the object. Always `web_search`."
        },
        "allowed_domains": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Domains the search may include."
        },
        "context_size": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "low",
            "medium",
            "high",
            null
          ],
          "description": "The amount of search context made available to the model. Defaults to `medium`."
        },
        "location": {
          "anyOf": [
            {
              "$ref": "#/definitions/PersistedAgentToolParam.PersistedAgentToolConfigParamWebSearch.Location"
            },
            {
              "type": "null"
            }
          ],
          "description": "Approximate location used to localize search results."
        },
        "mode": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "disabled",
            "cached",
            "live",
            null
          ],
          "description": "The source used for web search results. Defaults to `live`.\n\n- `disabled` - Disables web search.\n- `cached` - Uses cached search results.\n- `live` - Searches the live web."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Web search."
    },
    "PersistedAgentToolParam.PersistedAgentToolConfigParamWebSearch.Location": {
      "type": "object",
      "properties": {
        "city": {
          "type": [
            "string",
            "null"
          ],
          "description": "The city name."
        },
        "country": {
          "type": [
            "string",
            "null"
          ],
          "description": "The two-letter ISO country code, such as `US`."
        },
        "region": {
          "type": [
            "string",
            "null"
          ],
          "description": "The region or state name."
        },
        "timezone": {
          "type": [
            "string",
            "null"
          ],
          "description": "The IANA timezone, such as `America/Los_Angeles`."
        }
      },
      "additionalProperties": false,
      "description": "Approximate location used to localize search results."
    },
    "AgentUpdateParams": {
      "type": "object",
      "properties": {
        "instructions": {
          "type": [
            "string",
            "null"
          ],
          "description": "Additional instructions appended to the agent's default base instructions. Omit to leave unchanged."
        },
        "metadata": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Replaces all metadata. Omit to leave unchanged, or pass null or {} to clear it. Up to 16 string key-value pairs, with keys up to 64 and values up to 512 characters."
        },
        "model": {
          "type": "string",
          "description": "The model to use for the agent. The requested model name is preserved."
        },
        "multi_agent": {
          "anyOf": [
            {
              "$ref": "#/definitions/MultiAgentConfigParam"
            },
            {
              "type": "null"
            }
          ],
          "description": "Configuration for creating and coordinating subagents."
        },
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "A replacement name. Omit to leave unchanged, or pass null to clear it."
        },
        "reasoning": {
          "anyOf": [
            {
              "$ref": "#/definitions/AgentReasoningParam"
            },
            {
              "type": "null"
            }
          ],
          "description": "Configuration for model reasoning. Omit to keep the current settings; pass `null` to reset to the model's default effort."
        },
        "service_tier": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "auto",
            "default",
            "flex",
            "priority",
            "fast",
            null
          ],
          "description": "The service tier used for model requests.\n\n- `auto` - Selects the service tier automatically.\n- `default` - Uses the default service tier.\n- `flex` - Uses the flex service tier.\n- `priority` - Uses the priority service tier.\n- `fast` - Uses the fast service tier."
        },
        "text": {
          "anyOf": [
            {
              "$ref": "#/definitions/AgentTextParam"
            },
            {
              "type": "null"
            }
          ],
          "description": "Configuration for text generated by the agent."
        },
        "tools": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PersistedAgentToolParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Tools available to the agent."
        }
      },
      "additionalProperties": false
    },
    "AgentListParams": {
      "type": "object",
      "properties": {
        "after": {
          "type": "string"
        },
        "limit": {
          "type": [
            "number",
            "null"
          ],
          "description": "The maximum number of resources to return."
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "The order in which resources are returned. Defaults to `desc`.\n\n- `asc` - Returns resources in ascending order.\n- `desc` - Returns resources in descending order."
        }
      },
      "additionalProperties": false
    },
    "AgentDeleted": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the deleted agent."
        },
        "deleted": {
          "type": "boolean",
          "description": "Whether the agent was deleted. Always `true`."
        },
        "object": {
          "type": "string",
          "const": "agent.deleted",
          "description": "The object type. Always `agent.deleted`."
        }
      },
      "required": [
        "id",
        "deleted",
        "object"
      ],
      "additionalProperties": false,
      "description": "A deleted reusable agent."
    },
    "AgentSession": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the session."
        },
        "agent": {
          "$ref": "#/definitions/AgentSession.Agent",
          "description": "The agent running in the session."
        },
        "created_at": {
          "type": "number",
          "description": "The Unix timestamp, in seconds, when the session was created."
        },
        "environment": {
          "$ref": "#/definitions/Environment",
          "description": "The execution environment for the session."
        },
        "error": {
          "type": [
            "string",
            "null"
          ],
          "description": "The error that caused the session to fail, if any."
        },
        "last_active_at": {
          "type": "number",
          "description": "The Unix timestamp, in seconds, when the session was last active."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Custom string key-value pairs attached to the session."
        },
        "object": {
          "type": "string",
          "const": "agent.session",
          "description": "The object type. Always `agent.session`."
        },
        "required_actions": {
          "type": "array",
          "items": {
            "anyOf": [
              {
                "$ref": "#/definitions/AgentSession.SessionRequiredActionResourceFunctionCall"
              },
              {
                "$ref": "#/definitions/AgentSession.SessionRequiredActionResourceEnvironmentConnection"
              }
            ]
          },
          "description": "Actions that must be completed before the session can continue."
        },
        "status": {
          "type": "string",
          "enum": [
            "idle",
            "in_progress",
            "requires_action",
            "failed"
          ],
          "description": "The current status of the session.\n\n- `idle` - The session has no turn in progress and is ready for input. A hosted   environment may still be provisioning.\n- `in_progress` - The session is processing a turn.\n- `requires_action` - The session is waiting for one or more required actions.\n- `failed` - The session failed."
        },
        "usage": {
          "anyOf": [
            {
              "$ref": "#/definitions/TokenUsage"
            },
            {
              "type": "null"
            }
          ],
          "description": "Best-effort token usage for the session, or null if unknown. Recorded usage may change."
        },
        "vault_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The IDs of vaults made available to the session."
        }
      },
      "required": [
        "id",
        "agent",
        "created_at",
        "environment",
        "error",
        "last_active_at",
        "metadata",
        "object",
        "required_actions",
        "status",
        "usage",
        "vault_ids"
      ],
      "additionalProperties": false,
      "description": "A Managed Agents session."
    },
    "AgentSession.Agent": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the agent."
        },
        "instructions": {
          "type": [
            "string",
            "null"
          ],
          "description": "Custom instructions appended to the agent's default base instructions."
        },
        "model": {
          "type": "string",
          "description": "The model used by the agent."
        },
        "multi_agent": {
          "$ref": "#/definitions/MultiAgentConfig",
          "description": "Configuration for creating and coordinating subagents."
        },
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "The reusable agent's name when the session was created, or null if no name was saved. Later changes to the agent's name do not affect this value."
        },
        "reasoning": {
          "$ref": "#/definitions/AgentReasoning",
          "description": "The agent's reasoning configuration."
        },
        "service_tier": {
          "type": "string",
          "enum": [
            "auto",
            "default",
            "flex",
            "priority",
            "fast"
          ],
          "description": "The effective service-tier policy for model requests. Defaults to `auto`."
        },
        "text": {
          "$ref": "#/definitions/AgentText",
          "description": "Configuration for text generated by the agent."
        },
        "tools": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/AgentTool"
          },
          "description": "Tools available to the agent."
        }
      },
      "required": [
        "id",
        "instructions",
        "model",
        "multi_agent",
        "name",
        "reasoning",
        "service_tier",
        "text",
        "tools"
      ],
      "additionalProperties": false,
      "description": "The agent running in the session."
    },
    "AgentTool": {
      "anyOf": [
        {
          "$ref": "#/definitions/AgentTool.AgentToolResourceFunction"
        },
        {
          "$ref": "#/definitions/AgentTool.AgentToolResourceProgrammaticToolCalling"
        },
        {
          "$ref": "#/definitions/AgentTool.AgentToolResourceMcp"
        },
        {
          "$ref": "#/definitions/AgentTool.AgentToolResourceWebSearch"
        }
      ],
      "description": "A tool available to the agent."
    },
    "AgentTool.AgentToolResourceFunction": {
      "type": "object",
      "properties": {
        "defer_loading": {
          "type": "boolean",
          "description": "Whether the function is deferred and discovered through tool search."
        },
        "description": {
          "type": "string",
          "description": "A description of what the function does."
        },
        "name": {
          "type": "string",
          "description": "The name of the function."
        },
        "parameters": {
          "type": "object",
          "additionalProperties": {},
          "description": "A JSON Schema object describing the function's arguments."
        },
        "type": {
          "type": "string",
          "const": "function",
          "description": "The type of the object. Always `function`."
        }
      },
      "required": [
        "defer_loading",
        "description",
        "name",
        "parameters",
        "type"
      ],
      "additionalProperties": false,
      "description": "A function defined by the application."
    },
    "AgentTool.AgentToolResourceProgrammaticToolCalling": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Whether tools can be called from model-generated code."
        },
        "type": {
          "type": "string",
          "const": "programmatic_tool_calling",
          "description": "The type of the object. Always `programmatic_tool_calling`."
        }
      },
      "required": [
        "enabled",
        "type"
      ],
      "additionalProperties": false,
      "description": "Enables calling tools from model-generated code."
    },
    "AgentTool.AgentToolResourceMcp": {
      "type": "object",
      "properties": {
        "allowed_tools": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "The MCP tools the agent may call."
        },
        "connection_origin": {
          "type": "string",
          "enum": [
            "service",
            "environment"
          ],
          "description": "Where outbound MCP HTTP connections originate."
        },
        "credential_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The attached vault credential selected for this MCP server, if any. Optional when exactly one attached credential matches the server URL."
        },
        "request_metadata": {
          "type": "object",
          "additionalProperties": {},
          "description": "Metadata included with requests to this MCP server."
        },
        "required": {
          "type": "boolean",
          "description": "Whether this MCP server must initialize before the first turn."
        },
        "server_label": {
          "type": "string",
          "description": "A label used to identify the MCP server in tool calls."
        },
        "transport": {
          "$ref": "#/definitions/McpTransport",
          "description": "The transport used to connect to the MCP server."
        },
        "type": {
          "type": "string",
          "const": "mcp",
          "description": "The type of the object. Always `mcp`."
        }
      },
      "required": [
        "allowed_tools",
        "connection_origin",
        "credential_id",
        "request_metadata",
        "required",
        "server_label",
        "transport",
        "type"
      ],
      "additionalProperties": false,
      "description": "Tools provided by a remote MCP server."
    },
    "McpTransport": {
      "anyOf": [
        {
          "$ref": "#/definitions/McpTransport.McpTransportResourceHTTP"
        },
        {
          "$ref": "#/definitions/McpTransport.McpTransportResourceStdio"
        }
      ],
      "description": "The transport used to connect to an MCP server."
    },
    "McpTransport.McpTransportResourceHTTP": {
      "type": "object",
      "properties": {
        "server_url": {
          "type": "string",
          "description": "The URL of the MCP server."
        },
        "type": {
          "type": "string",
          "const": "http",
          "description": "The type of the object. Always `http`."
        }
      },
      "required": [
        "server_url",
        "type"
      ],
      "additionalProperties": false,
      "description": "Connects to an MCP server over HTTP."
    },
    "McpTransport.McpTransportResourceStdio": {
      "type": "object",
      "properties": {
        "args": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Arguments passed to the MCP server command."
        },
        "command": {
          "type": "string",
          "description": "The command used to start the MCP server."
        },
        "cwd": {
          "type": "string",
          "description": "The working directory used to start the MCP server."
        },
        "env_vars": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Environment variable names inherited from the execution environment."
        },
        "type": {
          "type": "string",
          "const": "stdio",
          "description": "The type of the object. Always `stdio`."
        }
      },
      "required": [
        "args",
        "command",
        "cwd",
        "env_vars",
        "type"
      ],
      "additionalProperties": false,
      "description": "Starts an MCP server as a local process."
    },
    "AgentTool.AgentToolResourceWebSearch": {
      "type": "object",
      "properties": {
        "allowed_domains": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Allowed search domains, or `null` when the search is unrestricted."
        },
        "context_size": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "description": "The amount of search context made available to the model. Defaults to `medium`."
        },
        "location": {
          "anyOf": [
            {
              "$ref": "#/definitions/AgentTool.AgentToolResourceWebSearch.Location"
            },
            {
              "type": "null"
            }
          ],
          "description": "Approximate location used to localize search results, if provided."
        },
        "mode": {
          "type": "string",
          "enum": [
            "disabled",
            "cached",
            "live"
          ],
          "description": "The source used for web search results."
        },
        "type": {
          "type": "string",
          "const": "web_search",
          "description": "The type of the object. Always `web_search`."
        }
      },
      "required": [
        "allowed_domains",
        "context_size",
        "location",
        "mode",
        "type"
      ],
      "additionalProperties": false,
      "description": "Web search."
    },
    "AgentTool.AgentToolResourceWebSearch.Location": {
      "type": "object",
      "properties": {
        "city": {
          "type": [
            "string",
            "null"
          ],
          "description": "The city name."
        },
        "country": {
          "type": [
            "string",
            "null"
          ],
          "description": "The two-letter ISO country code, such as `US`."
        },
        "region": {
          "type": [
            "string",
            "null"
          ],
          "description": "The region or state name."
        },
        "timezone": {
          "type": [
            "string",
            "null"
          ],
          "description": "The IANA timezone, such as `America/Los_Angeles`."
        }
      },
      "required": [
        "city",
        "country",
        "region",
        "timezone"
      ],
      "additionalProperties": false,
      "description": "Approximate location used to localize search results, if provided."
    },
    "Environment": {
      "anyOf": [
        {
          "$ref": "#/definitions/Environment.EnvironmentResourceNone"
        },
        {
          "$ref": "#/definitions/Environment.EnvironmentResourceOpenAIHosted"
        },
        {
          "$ref": "#/definitions/Environment.EnvironmentResourceSelfHosted"
        }
      ],
      "description": "The execution environment for a session."
    },
    "Environment.EnvironmentResourceNone": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "none",
          "description": "The type of the object. Always `none`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "The session talks to CCA without selecting or provisioning an execution environment."
    },
    "Environment.EnvironmentResourceOpenAIHosted": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The public ID of the environment."
        },
        "capability_directories": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Directories that contain capabilities exposed to the agent."
        },
        "files": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HostedEnvironmentFile"
          },
          "description": "Files available in the environment, excluding their contents."
        },
        "network": {
          "$ref": "#/definitions/Environment.EnvironmentResourceOpenAIHosted.Network",
          "description": "The effective network access policy for the environment."
        },
        "packages": {
          "$ref": "#/definitions/Environment.EnvironmentResourceOpenAIHosted.Packages",
          "description": "Packages installed in the environment."
        },
        "plugins": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HostedPlugin"
          },
          "description": "Plugins installed in the environment, excluding their archive contents."
        },
        "skills": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HostedSkill"
          },
          "description": "Skills installed in the environment, excluding their archive contents."
        },
        "type": {
          "type": "string",
          "const": "openai_hosted",
          "description": "The type of the object. Always `openai_hosted`."
        }
      },
      "required": [
        "id",
        "capability_directories",
        "files",
        "network",
        "packages",
        "plugins",
        "skills",
        "type"
      ],
      "additionalProperties": false,
      "description": "An environment hosted by OpenAI."
    },
    "HostedEnvironmentFile": {
      "anyOf": [
        {
          "$ref": "#/definitions/HostedEnvironmentFileID"
        },
        {
          "$ref": "#/definitions/HostedEnvironmentFile.HostedEnvironmentFileResourceInline"
        }
      ],
      "description": "Metadata for a file materialized in an OpenAI-hosted execution environment."
    },
    "HostedEnvironmentFileID": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The session-scoped ID of the file in the execution environment."
        },
        "file_id": {
          "type": "string",
          "description": "The ID of the uploaded file."
        },
        "path": {
          "type": "string",
          "description": "The file's absolute path inside the environment."
        },
        "size_bytes": {
          "type": "number",
          "description": "The decoded file size in bytes."
        },
        "type": {
          "type": "string",
          "const": "file_id",
          "description": "The type of the object. Always `file_id`."
        }
      },
      "required": [
        "id",
        "file_id",
        "path",
        "size_bytes",
        "type"
      ],
      "additionalProperties": false,
      "description": "A file copied from the OpenAI Files API."
    },
    "HostedEnvironmentFile.HostedEnvironmentFileResourceInline": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The session-scoped ID of the file in the execution environment."
        },
        "path": {
          "type": "string",
          "description": "The file's absolute path inside the environment."
        },
        "size_bytes": {
          "type": "number",
          "description": "The decoded file size in bytes."
        },
        "type": {
          "type": "string",
          "const": "inline",
          "description": "The type of the object. Always `inline`."
        }
      },
      "required": [
        "id",
        "path",
        "size_bytes",
        "type"
      ],
      "additionalProperties": false,
      "description": "A file supplied inline when the session was created."
    },
    "Environment.EnvironmentResourceOpenAIHosted.Network": {
      "type": "object",
      "properties": {
        "access": {
          "type": "string",
          "enum": [
            "enabled",
            "disabled",
            "restricted"
          ],
          "description": "The environment's network access mode.\n\n- `enabled` - Allows unrestricted network access.\n- `disabled` - Disables network access.\n- `restricted` - Allows access only to configured domains."
        },
        "allowed_domains": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Domains the environment may access when network access is restricted."
        }
      },
      "required": [
        "access",
        "allowed_domains"
      ],
      "additionalProperties": false,
      "description": "The effective network access policy for the environment."
    },
    "Environment.EnvironmentResourceOpenAIHosted.Packages": {
      "type": "object",
      "properties": {
        "npm": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "npm packages installed globally in the environment."
        },
        "python": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Python packages installed in the environment."
        },
        "system": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "System packages installed in the environment."
        }
      },
      "required": [
        "npm",
        "python",
        "system"
      ],
      "additionalProperties": false,
      "description": "Packages installed in the environment."
    },
    "HostedPlugin": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string",
          "description": "The installed plugin description."
        },
        "name": {
          "type": "string",
          "description": "The installed plugin name."
        },
        "type": {
          "type": "string",
          "const": "inline",
          "description": "The type of the object. Always `inline`."
        }
      },
      "required": [
        "description",
        "name",
        "type"
      ],
      "additionalProperties": false,
      "description": "A plugin installed from an inline ZIP archive."
    },
    "HostedSkill": {
      "anyOf": [
        {
          "$ref": "#/definitions/HostedSkillReference"
        },
        {
          "$ref": "#/definitions/HostedSkill.HostedSkillResourceInline"
        }
      ],
      "description": "A skill installed in an OpenAI-hosted environment."
    },
    "HostedSkillReference": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string",
          "description": "The installed skill description."
        },
        "name": {
          "type": "string",
          "description": "The installed skill name."
        },
        "skill_id": {
          "type": "string",
          "description": "The referenced skill ID."
        },
        "type": {
          "type": "string",
          "const": "skill_reference",
          "description": "The type of the object. Always `skill_reference`."
        },
        "version": {
          "type": "string",
          "description": "The concrete skill version installed for this session."
        }
      },
      "required": [
        "description",
        "name",
        "skill_id",
        "type",
        "version"
      ],
      "additionalProperties": false,
      "description": "A skill installed from the Skills API."
    },
    "HostedSkill.HostedSkillResourceInline": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string",
          "description": "The installed skill description."
        },
        "name": {
          "type": "string",
          "description": "The installed skill name."
        },
        "type": {
          "type": "string",
          "const": "inline",
          "description": "The type of the object. Always `inline`."
        }
      },
      "required": [
        "description",
        "name",
        "type"
      ],
      "additionalProperties": false,
      "description": "A skill installed from an inline ZIP archive."
    },
    "Environment.EnvironmentResourceSelfHosted": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The public ID of the environment."
        },
        "capability_directories": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Directories that contain capabilities exposed to the agent."
        },
        "remote_url": {
          "type": "string",
          "description": "Pass this URL unchanged to `codex exec-server --remote` when connecting this environment."
        },
        "type": {
          "type": "string",
          "const": "self_hosted",
          "description": "The type of the object. Always `self_hosted`."
        },
        "workspace_directory": {
          "type": "string",
          "description": "The absolute project directory inside the environment. Defaults to `/workspace`."
        }
      },
      "required": [
        "id",
        "capability_directories",
        "remote_url",
        "type",
        "workspace_directory"
      ],
      "additionalProperties": false,
      "description": "An environment hosted by the application."
    },
    "AgentSession.SessionRequiredActionResourceFunctionCall": {
      "type": "object",
      "properties": {
        "arguments": {
          "description": "The arguments supplied by the model."
        },
        "call_id": {
          "type": "string",
          "description": "The ID to include when submitting the function result."
        },
        "name": {
          "type": "string",
          "description": "The function name."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that requested the function call."
        },
        "type": {
          "type": "string",
          "const": "function_call",
          "description": "The type of the object. Always `function_call`."
        }
      },
      "required": [
        "arguments",
        "call_id",
        "name",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Run a function tool and submit its result."
    },
    "AgentSession.SessionRequiredActionResourceEnvironmentConnection": {
      "type": "object",
      "properties": {
        "environment_id": {
          "type": "string",
          "description": "The ID of the environment to reconnect."
        },
        "type": {
          "type": "string",
          "const": "environment_connection",
          "description": "The type of the object. Always `environment_connection`."
        }
      },
      "required": [
        "environment_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Reconnect a session environment."
    },
    "TokenUsage": {
      "type": "object",
      "properties": {
        "input_tokens": {
          "type": "number",
          "description": "The number of input tokens used by the agent."
        },
        "input_tokens_details": {
          "$ref": "#/definitions/TokenUsage.InputTokensDetails",
          "description": "A breakdown of the agent's input token usage."
        },
        "output_tokens": {
          "type": "number",
          "description": "The number of output tokens generated by the agent."
        },
        "output_tokens_details": {
          "$ref": "#/definitions/TokenUsage.OutputTokensDetails",
          "description": "A breakdown of the agent's output token usage."
        },
        "total_tokens": {
          "type": "number",
          "description": "The total number of input and output tokens used by the agent."
        }
      },
      "required": [
        "input_tokens",
        "input_tokens_details",
        "output_tokens",
        "output_tokens_details",
        "total_tokens"
      ],
      "additionalProperties": false,
      "description": "Recorded token usage for a session or turn. Usage is best effort and may change."
    },
    "TokenUsage.InputTokensDetails": {
      "type": "object",
      "properties": {
        "cached_tokens": {
          "type": "number",
          "description": "The number of input tokens retrieved from the prompt cache."
        }
      },
      "required": [
        "cached_tokens"
      ],
      "additionalProperties": false,
      "description": "A breakdown of the agent's input token usage."
    },
    "TokenUsage.OutputTokensDetails": {
      "type": "object",
      "properties": {
        "reasoning_tokens": {
          "type": "number",
          "description": "The number of output tokens used for reasoning."
        }
      },
      "required": [
        "reasoning_tokens"
      ],
      "additionalProperties": false,
      "description": "A breakdown of the agent's output token usage."
    },
    "SessionCreateParams": {
      "anyOf": [
        {
          "$ref": "#/definitions/SessionCreateParamsNonStreaming"
        },
        {
          "$ref": "#/definitions/SessionCreateParamsStreaming"
        }
      ]
    },
    "SessionCreateParamsNonStreaming": {
      "type": "object",
      "properties": {
        "environment": {
          "$ref": "#/definitions/EnvironmentParam",
          "description": "An inline execution environment or a reference to an environment template."
        },
        "agent": {
          "$ref": "#/definitions/SessionCreateParams.Agent",
          "description": "Agent configuration. With `agent_id`, supplied fields override the saved agent for this session. Without `agent_id`, `model` is required."
        },
        "agent_id": {
          "type": "string",
          "description": "The ID of a saved reusable agent. Omit `agent` to use its configuration unchanged."
        },
        "input": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AgentSessionInputMessageParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Initial input to submit when the session is created. A string is shorthand for a single user message. Required when `environment.type` is `none`, or when `stream` is `true` for an environment that is not `self_hosted`; optional for self-hosted and non-streaming execution environments."
        },
        "metadata": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Up to 16 string key-value pairs, with keys up to 64 and values up to 512 characters. Omission or null defaults to an empty map."
        },
        "stream": {
          "type": "boolean",
          "const": false,
          "description": "Whether to stream session events as server-sent events. Defaults to `false`."
        },
        "vault_ids": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "The IDs of vaults made available to the session."
        }
      },
      "additionalProperties": false,
      "required": [
        "environment"
      ]
    },
    "EnvironmentParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/EnvironmentParam.EnvironmentParamNone"
        },
        {
          "$ref": "#/definitions/EnvironmentParam.EnvironmentParamOpenAIHosted"
        },
        {
          "$ref": "#/definitions/EnvironmentParam.EnvironmentParamSelfHosted"
        }
      ],
      "description": "The execution environment and optional reusable template for a session."
    },
    "EnvironmentParam.EnvironmentParamNone": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "none",
          "description": "The type of the object. Always `none`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Runs the agent without an execution environment."
    },
    "EnvironmentParam.EnvironmentParamOpenAIHosted": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "openai_hosted",
          "description": "The type of the object. Always `openai_hosted`."
        },
        "capability_directories": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Directories that contain capabilities exposed to the agent. Defaults to an empty list."
        },
        "env": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Environment variables made available to the agent."
        },
        "environment_template_id": {
          "type": "string",
          "description": "A reusable hosted template applied before inline session configuration. Omitted fields inherit the template; network overrides cannot broaden its policy."
        },
        "files": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HostedEnvironmentFileParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Files available before the agent starts. Defaults to an empty list."
        },
        "network": {
          "anyOf": [
            {
              "$ref": "#/definitions/EnvironmentParam.EnvironmentParamOpenAIHosted.Network"
            },
            {
              "type": "null"
            }
          ],
          "description": "Network access policy for the environment. Defaults to disabled for GA requests and enabled for alpha/beta requests."
        },
        "packages": {
          "anyOf": [
            {
              "$ref": "#/definitions/EnvironmentParam.EnvironmentParamOpenAIHosted.Packages"
            },
            {
              "type": "null"
            }
          ],
          "description": "Packages to install in the environment. Defaults to empty package lists."
        },
        "plugins": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HostedPluginParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Plugins provided as inline ZIP archives. Defaults to an empty list."
        },
        "setup_commands": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SetupCommandParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Ordered, confidential setup commands. Command bodies are never returned."
        },
        "skills": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HostedSkillParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Skills referenced by ID or provided as inline ZIP archives. Defaults to an empty list."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "An existing OpenAI-hosted environment or new inline/template-based hosted configuration."
    },
    "HostedEnvironmentFileParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/HostedEnvironmentFileParam.HostedEnvironmentFileParamFileID"
        },
        {
          "$ref": "#/definitions/HostedEnvironmentFileParam.HostedEnvironmentFileParamInline"
        }
      ],
      "description": "A file materialized in an OpenAI-hosted execution environment."
    },
    "HostedEnvironmentFileParam.HostedEnvironmentFileParamFileID": {
      "type": "object",
      "properties": {
        "file_id": {
          "type": "string",
          "description": "The ID of the uploaded file."
        },
        "path": {
          "type": "string",
          "description": "The absolute destination path inside `/workspace`."
        },
        "type": {
          "type": "string",
          "const": "file_id",
          "description": "The type of the object. Always `file_id`."
        }
      },
      "required": [
        "file_id",
        "path",
        "type"
      ],
      "additionalProperties": false,
      "description": "A file previously uploaded through the OpenAI Files API."
    },
    "HostedEnvironmentFileParam.HostedEnvironmentFileParamInline": {
      "type": "object",
      "properties": {
        "data": {
          "type": "string",
          "description": "The standard-base64-encoded file contents."
        },
        "path": {
          "type": "string",
          "description": "The absolute destination path inside `/workspace`."
        },
        "type": {
          "type": "string",
          "const": "inline",
          "description": "The type of the object. Always `inline`."
        }
      },
      "required": [
        "data",
        "path",
        "type"
      ],
      "additionalProperties": false,
      "description": "A file supplied directly as standard-base64 data."
    },
    "EnvironmentParam.EnvironmentParamOpenAIHosted.Network": {
      "type": "object",
      "properties": {
        "access": {
          "type": "string",
          "enum": [
            "enabled",
            "disabled",
            "restricted"
          ],
          "description": "The environment's network access mode.\n\n- `enabled` - Allows unrestricted network access.\n- `disabled` - Disables network access.\n- `restricted` - Allows access only to configured domains."
        },
        "allowed_domains": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Domains the environment may access when network access is restricted."
        }
      },
      "required": [
        "access"
      ],
      "additionalProperties": false,
      "description": "Network access policy for the environment. Defaults to disabled for GA requests and enabled for alpha/beta requests."
    },
    "EnvironmentParam.EnvironmentParamOpenAIHosted.Packages": {
      "type": "object",
      "properties": {
        "npm": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "npm packages to install globally. Defaults to an empty list."
        },
        "python": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Python packages to install. Defaults to an empty list."
        },
        "system": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "System packages to install. Defaults to an empty list."
        }
      },
      "additionalProperties": false,
      "description": "Packages to install in the environment. Defaults to empty package lists."
    },
    "HostedPluginParam": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string",
          "description": "The plugin description declared in `.codex-plugin/plugin.json`."
        },
        "name": {
          "type": "string",
          "description": "The plugin name declared in `.codex-plugin/plugin.json`."
        },
        "source": {
          "$ref": "#/definitions/InlineCapabilitySourceParam",
          "description": "Provides ZIP bytes encoded with standard base64."
        },
        "type": {
          "type": "string",
          "const": "inline",
          "description": "The type of the object. Always `inline`."
        }
      },
      "required": [
        "description",
        "name",
        "source",
        "type"
      ],
      "additionalProperties": false,
      "description": "Supplies a plugin ZIP directly in the session request."
    },
    "InlineCapabilitySourceParam": {
      "type": "object",
      "properties": {
        "data": {
          "type": "string",
          "description": "Standard-base64 encoded ZIP archive bytes."
        },
        "media_type": {
          "type": "string",
          "const": "application/zip",
          "description": "The archive media type, always `application/zip`."
        },
        "type": {
          "type": "string",
          "const": "base64",
          "description": "The type of the object. Always `base64`."
        }
      },
      "required": [
        "data",
        "media_type",
        "type"
      ],
      "additionalProperties": false,
      "description": "Provides ZIP bytes encoded with standard base64."
    },
    "SetupCommandParam": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "The shell command to execute."
        },
        "cwd": {
          "type": [
            "string",
            "null"
          ],
          "description": "The absolute working directory. Defaults to `/workspace`."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false,
      "description": "A confidential setup command executed before the hosted agent starts."
    },
    "HostedSkillParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/HostedSkillParam.HostedSkillParamSkillReference"
        },
        {
          "$ref": "#/definitions/HostedSkillParam.HostedSkillParamInline"
        }
      ],
      "description": "A skill installed in an OpenAI-hosted environment."
    },
    "HostedSkillParam.HostedSkillParamSkillReference": {
      "type": "object",
      "properties": {
        "skill_id": {
          "type": "string",
          "description": "The ID of the skill created through `/v1/skills`."
        },
        "type": {
          "type": "string",
          "const": "skill_reference",
          "description": "The type of the object. Always `skill_reference`."
        },
        "version": {
          "type": [
            "string",
            "null"
          ],
          "description": "The skill version, a positive integer or `latest`; omission selects the default."
        }
      },
      "required": [
        "skill_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "References a skill uploaded through the Skills API."
    },
    "HostedSkillParam.HostedSkillParamInline": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string",
          "description": "The skill description declared in `SKILL.md`."
        },
        "name": {
          "type": "string",
          "description": "The skill name declared in `SKILL.md`."
        },
        "source": {
          "$ref": "#/definitions/InlineCapabilitySourceParam",
          "description": "Provides ZIP bytes encoded with standard base64."
        },
        "type": {
          "type": "string",
          "const": "inline",
          "description": "The type of the object. Always `inline`."
        }
      },
      "required": [
        "description",
        "name",
        "source",
        "type"
      ],
      "additionalProperties": false,
      "description": "Supplies a skill ZIP directly in the session request."
    },
    "EnvironmentParam.EnvironmentParamSelfHosted": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "self_hosted",
          "description": "The type of the object. Always `self_hosted`."
        },
        "workspace_directory": {
          "type": "string",
          "description": "Absolute project directory inside the self-hosted environment."
        },
        "capability_directories": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Directories that contain capabilities exposed to the agent. Defaults to an empty list."
        }
      },
      "required": [
        "type",
        "workspace_directory"
      ],
      "additionalProperties": false,
      "description": "An application-hosted environment configured inline."
    },
    "SessionCreateParams.Agent": {
      "type": "object",
      "properties": {
        "instructions": {
          "type": [
            "string",
            "null"
          ],
          "description": "Additional instructions appended to the agent's default base instructions. Omit to leave unchanged."
        },
        "model": {
          "type": "string",
          "description": "The model to use for the agent. The requested model name is preserved."
        },
        "multi_agent": {
          "anyOf": [
            {
              "$ref": "#/definitions/MultiAgentConfigParam"
            },
            {
              "type": "null"
            }
          ],
          "description": "Configuration for creating and coordinating subagents."
        },
        "reasoning": {
          "anyOf": [
            {
              "$ref": "#/definitions/AgentReasoningParam"
            },
            {
              "type": "null"
            }
          ],
          "description": "Configuration for model reasoning. Omit to keep the current settings; pass `null` to reset to the model's default effort."
        },
        "service_tier": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "auto",
            "default",
            "flex",
            "priority",
            "fast",
            null
          ],
          "description": "The service tier used for model requests.\n\n- `auto` - Selects the service tier automatically.\n- `default` - Uses the default service tier.\n- `flex` - Uses the flex service tier.\n- `priority` - Uses the priority service tier.\n- `fast` - Uses the fast service tier."
        },
        "text": {
          "anyOf": [
            {
              "$ref": "#/definitions/AgentTextParam"
            },
            {
              "type": "null"
            }
          ],
          "description": "Configuration for text generated by the agent."
        },
        "tools": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AgentToolParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Tools available to the agent. Omit to inherit, or pass null to clear them."
        }
      },
      "additionalProperties": false,
      "description": "Agent configuration. With `agent_id`, supplied fields override the saved agent for this session. Without `agent_id`, `model` is required."
    },
    "AgentToolParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/AgentToolParam.AgentToolConfigParamFunction"
        },
        {
          "$ref": "#/definitions/AgentToolParam.AgentToolConfigParamToolSearch"
        },
        {
          "$ref": "#/definitions/AgentToolParam.AgentToolConfigParamProgrammaticToolCalling"
        },
        {
          "$ref": "#/definitions/AgentToolParam.AgentToolConfigParamMcp"
        },
        {
          "$ref": "#/definitions/AgentToolParam.AgentToolConfigParamWebSearch"
        }
      ],
      "description": "A tool available to the agent."
    },
    "AgentToolParam.AgentToolConfigParamFunction": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string",
          "description": "A description of what the function does."
        },
        "name": {
          "type": "string",
          "description": "The name of the function."
        },
        "parameters": {
          "type": "object",
          "additionalProperties": {},
          "description": "A JSON Schema object describing the function's arguments."
        },
        "type": {
          "type": "string",
          "const": "function",
          "description": "The type of the object. Always `function`."
        },
        "defer_loading": {
          "type": "boolean",
          "description": "Whether this function is deferred and discovered through tool search. Defaults to `false`."
        }
      },
      "required": [
        "description",
        "name",
        "parameters",
        "type"
      ],
      "additionalProperties": false,
      "description": "A function defined by the application."
    },
    "AgentToolParam.AgentToolConfigParamToolSearch": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "tool_search",
          "description": "The type of the object. Always `tool_search`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Discovers deferred function tools and loads them into the model context."
    },
    "AgentToolParam.AgentToolConfigParamProgrammaticToolCalling": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "programmatic_tool_calling",
          "description": "The type of the object. Always `programmatic_tool_calling`."
        },
        "enabled": {
          "type": "boolean",
          "description": "Whether tools can be called from model-generated code. Defaults to `true`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Enables calling tools from model-generated code."
    },
    "AgentToolParam.AgentToolConfigParamMcp": {
      "type": "object",
      "properties": {
        "server_label": {
          "type": "string",
          "description": "A label used to identify the MCP server in tool calls."
        },
        "transport": {
          "$ref": "#/definitions/McpTransportParam",
          "description": "The transport used to connect to the MCP server."
        },
        "type": {
          "type": "string",
          "const": "mcp",
          "description": "The type of the object. Always `mcp`."
        },
        "allowed_tools": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "The MCP tools the agent may call. All server tools are allowed when omitted."
        },
        "connection_origin": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "service",
            "environment",
            null
          ],
          "description": "Selects where outbound MCP HTTP connections originate. Omitted or `service` uses the Managed Agents service network; `environment` uses the session's selected environment.\n\n- `service` - Uses the Managed Agents service network.\n- `environment` - Uses the session's execution environment."
        },
        "credential_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The attached vault credential used to authenticate this MCP server. Optional when exactly one attached credential matches the server URL."
        },
        "request_metadata": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {}
            },
            {
              "type": "null"
            }
          ],
          "description": "Metadata included with requests to this MCP server."
        },
        "required": {
          "type": "boolean",
          "description": "Whether this MCP server must initialize before the first turn. Defaults to `false`."
        }
      },
      "required": [
        "server_label",
        "transport",
        "type"
      ],
      "additionalProperties": false,
      "description": "Tools provided by a remote MCP server."
    },
    "McpTransportParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/McpTransportParam.McpTransportConfigParamHTTP"
        },
        {
          "$ref": "#/definitions/McpTransportParam.McpTransportConfigParamStdio"
        }
      ],
      "description": "The transport used to connect to an MCP server."
    },
    "McpTransportParam.McpTransportConfigParamHTTP": {
      "type": "object",
      "properties": {
        "server_url": {
          "type": "string",
          "description": "The URL of the MCP server."
        },
        "type": {
          "type": "string",
          "const": "http",
          "description": "The type of the object. Always `http`."
        },
        "authorization": {
          "type": [
            "string",
            "null"
          ],
          "description": "The authorization value sent to the MCP server, if any."
        },
        "headers": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Additional HTTP headers sent to the MCP server."
        }
      },
      "required": [
        "server_url",
        "type"
      ],
      "additionalProperties": false,
      "description": "Connects to an MCP server over HTTP."
    },
    "McpTransportParam.McpTransportConfigParamStdio": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "The command used to start the MCP server."
        },
        "cwd": {
          "type": "string",
          "description": "The working directory used to start the MCP server."
        },
        "type": {
          "type": "string",
          "const": "stdio",
          "description": "The type of the object. Always `stdio`."
        },
        "args": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Arguments passed to the MCP server command."
        },
        "env": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Environment variables set for the MCP server process."
        },
        "env_vars": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Environment variable names to inherit from the selected execution environment."
        }
      },
      "required": [
        "command",
        "cwd",
        "type"
      ],
      "additionalProperties": false,
      "description": "Starts an MCP server as a local process."
    },
    "AgentToolParam.AgentToolConfigParamWebSearch": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "web_search",
          "description": "The type of the object. Always `web_search`."
        },
        "allowed_domains": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Domains the search may include."
        },
        "context_size": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "low",
            "medium",
            "high",
            null
          ],
          "description": "The amount of search context made available to the model. Defaults to `medium`."
        },
        "location": {
          "anyOf": [
            {
              "$ref": "#/definitions/AgentToolParam.AgentToolConfigParamWebSearch.Location"
            },
            {
              "type": "null"
            }
          ],
          "description": "Approximate location used to localize search results."
        },
        "mode": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "disabled",
            "cached",
            "live",
            null
          ],
          "description": "The source used for web search results. Defaults to `live`.\n\n- `disabled` - Disables web search.\n- `cached` - Uses cached search results.\n- `live` - Searches the live web."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Web search."
    },
    "AgentToolParam.AgentToolConfigParamWebSearch.Location": {
      "type": "object",
      "properties": {
        "city": {
          "type": [
            "string",
            "null"
          ],
          "description": "The city name."
        },
        "country": {
          "type": [
            "string",
            "null"
          ],
          "description": "The two-letter ISO country code, such as `US`."
        },
        "region": {
          "type": [
            "string",
            "null"
          ],
          "description": "The region or state name."
        },
        "timezone": {
          "type": [
            "string",
            "null"
          ],
          "description": "The IANA timezone, such as `America/Los_Angeles`."
        }
      },
      "additionalProperties": false,
      "description": "Approximate location used to localize search results."
    },
    "AgentSessionInputMessageParam": {
      "type": "object",
      "properties": {
        "content": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/InputContentParam"
          },
          "description": "The content of the message."
        },
        "role": {
          "type": "string",
          "const": "user",
          "description": "The role of the message author. Always `user`."
        },
        "type": {
          "type": "string",
          "const": "message",
          "description": "The type of the input item. Always `message`."
        }
      },
      "required": [
        "content",
        "role"
      ],
      "additionalProperties": false,
      "description": "A user message submitted to a session."
    },
    "InputContentParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/InputContentParam.InputContentParamInputText"
        },
        {
          "$ref": "#/definitions/InputContentParam.InputContentParamInputImage"
        }
      ],
      "description": "Content included in an input message."
    },
    "InputContentParam.InputContentParamInputText": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The text sent to the model."
        },
        "type": {
          "type": "string",
          "const": "input_text",
          "description": "The type of the object. Always `input_text`."
        }
      },
      "required": [
        "text",
        "type"
      ],
      "additionalProperties": false,
      "description": "Text input to the model."
    },
    "InputContentParam.InputContentParamInputImage": {
      "type": "object",
      "properties": {
        "image_url": {
          "type": "string",
          "description": "The URL of the image sent to the model."
        },
        "type": {
          "type": "string",
          "const": "input_image",
          "description": "The type of the object. Always `input_image`."
        }
      },
      "required": [
        "image_url",
        "type"
      ],
      "additionalProperties": false,
      "description": "Image input to the model."
    },
    "SessionCreateParamsStreaming": {
      "type": "object",
      "properties": {
        "environment": {
          "$ref": "#/definitions/EnvironmentParam",
          "description": "An inline execution environment or a reference to an environment template."
        },
        "agent": {
          "$ref": "#/definitions/SessionCreateParams.Agent",
          "description": "Agent configuration. With `agent_id`, supplied fields override the saved agent for this session. Without `agent_id`, `model` is required."
        },
        "agent_id": {
          "type": "string",
          "description": "The ID of a saved reusable agent. Omit `agent` to use its configuration unchanged."
        },
        "input": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AgentSessionInputMessageParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Initial input to submit when the session is created. A string is shorthand for a single user message. Required when `environment.type` is `none`, or when `stream` is `true` for an environment that is not `self_hosted`; optional for self-hosted and non-streaming execution environments."
        },
        "metadata": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Up to 16 string key-value pairs, with keys up to 64 and values up to 512 characters. Omission or null defaults to an empty map."
        },
        "stream": {
          "type": "boolean",
          "const": true,
          "description": "Whether to stream session events as server-sent events. Defaults to `false`."
        },
        "vault_ids": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "The IDs of vaults made available to the session."
        }
      },
      "required": [
        "environment",
        "stream"
      ],
      "additionalProperties": false
    },
    "SessionUpdateParams": {
      "type": "object",
      "properties": {
        "agent": {
          "$ref": "#/definitions/SessionUpdateParams.Agent",
          "description": "Model settings for subsequent turns. Omitted fields stay unchanged."
        },
        "metadata": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Replaces all metadata. Omit to leave unchanged, or pass null or {} to clear it. Up to 16 string key-value pairs, with keys up to 64 and values up to 512 characters."
        }
      },
      "additionalProperties": false
    },
    "SessionUpdateParams.Agent": {
      "type": "object",
      "properties": {
        "model": {
          "type": "string",
          "description": "The model for subsequent turns. Omit to keep the current model."
        },
        "reasoning": {
          "$ref": "#/definitions/SessionUpdateParams.Agent.Reasoning",
          "description": "Reasoning settings to update. Omit to keep the current effort."
        },
        "service_tier": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "auto",
            "default",
            "flex",
            "priority",
            "fast",
            null
          ],
          "description": "Omit to keep the current tier. Null resets it to auto.\n\n- `auto` - Selects the service tier automatically.\n- `default` - Uses the default service tier.\n- `flex` - Uses the flex service tier.\n- `priority` - Uses the priority service tier.\n- `fast` - Uses the fast service tier."
        }
      },
      "additionalProperties": false,
      "description": "Model settings for subsequent turns. Omitted fields stay unchanged."
    },
    "SessionUpdateParams.Agent.Reasoning": {
      "type": "object",
      "properties": {
        "effort": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "none",
            "minimal",
            "low",
            "medium",
            "high",
            "xhigh",
            "max",
            null
          ],
          "description": "Omit to keep the current effort. Null selects the model's default effort."
        }
      },
      "additionalProperties": false,
      "description": "Reasoning settings to update. Omit to keep the current effort."
    },
    "SessionListParams": {
      "type": "object",
      "properties": {
        "after": {
          "type": "string"
        },
        "agent_id": {
          "type": "string",
          "description": "Only return sessions whose root agent has this ID. Omit to return sessions for all agents."
        },
        "limit": {
          "type": [
            "number",
            "null"
          ],
          "description": "The maximum number of resources to return."
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "Sort order by the `created_at` timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `desc`.\n\n- `asc` - Returns resources in ascending order.\n- `desc` - Returns resources in descending order."
        }
      },
      "additionalProperties": false
    },
    "AgentSessionDeleted": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the deleted session."
        },
        "deleted": {
          "type": "boolean",
          "description": "Whether the session has been removed from the public API. Always `true`. Physical cleanup may still be in progress."
        },
        "object": {
          "type": "string",
          "const": "agent.session.deleted",
          "description": "The object type. Always `agent.session.deleted`."
        }
      },
      "required": [
        "id",
        "deleted",
        "object"
      ],
      "additionalProperties": false,
      "description": "A Managed Agents session removed from the public API. Physical cleanup may continue asynchronously."
    },
    "SessionEventsRequest": {
      "type": "object",
      "properties": {
        "events": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/AgentSessionInputParam"
          },
          "description": "Body param: The input events to submit to the session."
        }
      },
      "required": [
        "events"
      ],
      "additionalProperties": false
    },
    "AgentSessionInputParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/AgentSessionInputParam.SessionInputParamAgentSessionInputMessage"
        },
        {
          "$ref": "#/definitions/AgentSessionInputParam.SessionInputParamAgentSessionInputCancel"
        },
        {
          "$ref": "#/definitions/AgentSessionInputParam.SessionInputParamAgentSessionInputToolResult"
        }
      ],
      "description": "Input submitted to an existing session."
    },
    "AgentSessionInputParam.SessionInputParamAgentSessionInputMessage": {
      "type": "object",
      "properties": {
        "input": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/AgentSessionInputMessageParam"
          },
          "description": "The user messages to add to the session."
        },
        "type": {
          "type": "string",
          "const": "agent.session.input.message",
          "description": "The type of the object. Always `agent.session.input.message`."
        }
      },
      "required": [
        "input",
        "type"
      ],
      "additionalProperties": false,
      "description": "Adds one or more user messages and starts a turn."
    },
    "AgentSessionInputParam.SessionInputParamAgentSessionInputCancel": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "agent.session.input.cancel",
          "description": "The type of the object. Always `agent.session.input.cancel`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Cancels the session's active turn."
    },
    "AgentSessionInputParam.SessionInputParamAgentSessionInputToolResult": {
      "type": "object",
      "properties": {
        "call_id": {
          "type": "string",
          "description": "The ID of the function call."
        },
        "success": {
          "type": "boolean",
          "description": "Whether the function call succeeded."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that requested the function call."
        },
        "type": {
          "type": "string",
          "const": "agent.session.input.tool_result",
          "description": "The type of the object. Always `agent.session.input.tool_result`."
        },
        "error": {
          "type": [
            "string",
            "null"
          ],
          "description": "The error message when the call failed."
        },
        "output": {
          "anyOf": [
            {
              "$ref": "#/definitions/AgentFunctionCallOutputParam"
            },
            {
              "type": "null"
            }
          ],
          "description": "The function result when the call succeeded."
        }
      },
      "required": [
        "call_id",
        "success",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Submits the result of a function call."
    },
    "AgentFunctionCallOutputParam": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/InputContentParam"
          }
        }
      ],
      "description": "A function result represented as text or supported model-input content."
    },
    "AgentSessionEvent": {
      "anyOf": [
        {
          "$ref": "#/definitions/AgentSessionErrorEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionEnvironmentReadyEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionEnvironmentResetEvent"
        },
        {
          "$ref": "#/definitions/AgentOutputCommandExecutionOutputDeltaEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionCreatedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnCreatedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnInProgressEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnCompletedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnFailedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnCancelledEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnItemAddedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionIdleEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionInProgressEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionRequiresActionEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionFailedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionEnvironmentPendingEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionEnvironmentConnectedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionEnvironmentDisconnectedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionEnvironmentFailedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionSubagentCreatedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionSubagentActiveEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionSubagentClosedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnItemDoneEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnContentPartAddedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnContentPartDoneEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnOutputTextDeltaEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnOutputTextDoneEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnReasoningSummaryPartAddedEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnReasoningSummaryPartDoneEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnReasoningSummaryTextDeltaEvent"
        },
        {
          "$ref": "#/definitions/AgentSessionTurnReasoningSummaryTextDoneEvent"
        }
      ],
      "description": "An event emitted by a Managed Agents session."
    },
    "AgentSessionErrorEvent": {
      "type": "object",
      "properties": {
        "error": {
          "$ref": "#/definitions/SessionError",
          "description": "The error that occurred."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "type": {
          "type": "string",
          "const": "error",
          "description": "The type of the object. Always `error`."
        }
      },
      "required": [
        "error",
        "event_id",
        "session_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a turn or session fails."
    },
    "SessionError": {
      "type": "object",
      "properties": {
        "code": {
          "type": [
            "string",
            "null"
          ],
          "description": "The machine-readable error code, if any."
        },
        "message": {
          "type": "string",
          "description": "A customer-safe explanation of the error."
        },
        "param": {
          "type": [
            "string",
            "null"
          ],
          "description": "The request parameter associated with the error, if any."
        },
        "type": {
          "type": "string",
          "description": "The error type."
        }
      },
      "required": [
        "code",
        "message",
        "param",
        "type"
      ],
      "additionalProperties": false,
      "description": "An error payload with the same public fields as Responses API streaming errors."
    },
    "AgentSessionEnvironmentReadyEvent": {
      "type": "object",
      "properties": {
        "environment": {
          "$ref": "#/definitions/AgentSessionEnvironmentState",
          "description": "The current environment state."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.environment.ready",
          "description": "The type of the object. Always `agent.session.environment.ready`."
        }
      },
      "required": [
        "environment",
        "event_id",
        "session_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a hosted session environment is ready to connect."
    },
    "AgentSessionEnvironmentState": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The public ID of the environment."
        },
        "error": {
          "anyOf": [
            {
              "$ref": "#/definitions/AgentSessionEnvironmentState.Error"
            },
            {
              "type": "null"
            }
          ],
          "description": "The error reported while preparing the environment, if any."
        },
        "status": {
          "type": "string",
          "enum": [
            "pending",
            "ready",
            "connected",
            "disconnected",
            "failed"
          ],
          "description": "The environment's connection status.\n\n- `pending` - The environment is being prepared.\n- `ready` - The environment is ready to connect.\n- `connected` - The environment is connected.\n- `disconnected` - The environment is disconnected.\n- `failed` - The environment failed to connect."
        },
        "type": {
          "type": "string",
          "description": "The environment type."
        }
      },
      "required": [
        "id",
        "error",
        "status",
        "type"
      ],
      "additionalProperties": false,
      "description": "The current state of a session environment."
    },
    "AgentSessionEnvironmentState.Error": {
      "type": "object",
      "properties": {
        "code": {
          "type": "string",
          "description": "A machine-readable error code."
        },
        "message": {
          "type": "string",
          "description": "A human-readable error message."
        },
        "type": {
          "type": "string",
          "description": "The error type."
        }
      },
      "required": [
        "code",
        "message",
        "type"
      ],
      "additionalProperties": false,
      "description": "The error reported while preparing the environment, if any."
    },
    "AgentSessionEnvironmentResetEvent": {
      "type": "object",
      "properties": {
        "environment_id": {
          "type": "string",
          "description": "The stable environment ID, retained across sandbox replacements."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "reset_count": {
          "type": "number",
          "description": "Monotonically increasing reset number. Repeated notifications share this number."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The associated turn, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.environment.reset",
          "description": "The type of the object. Always `agent.session.environment.reset`."
        }
      },
      "required": [
        "environment_id",
        "event_id",
        "reset_count",
        "session_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted after a hosted sandbox is replaced. Conversation history survives; changes to the previous sandbox's files and processes do not."
    },
    "AgentOutputCommandExecutionOutputDeltaEvent": {
      "type": "object",
      "properties": {
        "delta": {
          "type": "string",
          "description": "The output text that was appended."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "item_id": {
          "type": "string",
          "description": "The ID of the command execution item."
        },
        "output_index": {
          "type": "number",
          "description": "The index of the item in the turn output."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.output.command_execution_output.delta",
          "description": "The type of the object. Always `agent.output.command_execution_output.delta`."
        }
      },
      "required": [
        "delta",
        "event_id",
        "item_id",
        "output_index",
        "session_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when command execution produces an output delta."
    },
    "AgentSessionCreatedEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session": {
          "$ref": "#/definitions/AgentSession",
          "description": "The session that was created."
        },
        "type": {
          "type": "string",
          "const": "agent.session.created",
          "description": "The type of the object. Always `agent.session.created`."
        }
      },
      "required": [
        "event_id",
        "session",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a session is created."
    },
    "AgentSessionTurnCreatedEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn": {
          "$ref": "#/definitions/Turn",
          "description": "The turn at the time it was created."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn associated with the event."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.created",
          "description": "The type of the object. Always `agent.session.turn.created`."
        }
      },
      "required": [
        "event_id",
        "session_id",
        "turn",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a turn is created."
    },
    "Turn": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the turn."
        },
        "agent_id": {
          "type": "string",
          "description": "The ID of the agent that ran the turn."
        },
        "completed_at": {
          "type": [
            "number",
            "null"
          ],
          "description": "The Unix timestamp, in seconds, when the turn reached a terminal state."
        },
        "created_at": {
          "type": "number",
          "description": "The Unix timestamp, in seconds, used to order the turn by creation time. Subagent turns use their start time, falling back to completion time or the subagent opening time when the preceding timestamps are unavailable."
        },
        "error": {
          "anyOf": [
            {
              "$ref": "#/definitions/SessionTurnError"
            },
            {
              "type": "null"
            }
          ],
          "description": "A customer-safe error. Non-null only for a failed turn."
        },
        "object": {
          "type": "string",
          "const": "agent.session.turn",
          "description": "The object type. Always `agent.session.turn`."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session that owns the turn."
        },
        "started_at": {
          "type": [
            "number",
            "null"
          ],
          "description": "The Unix timestamp, in seconds, when the turn started."
        },
        "status": {
          "type": "string",
          "enum": [
            "queued",
            "in_progress",
            "waiting",
            "completed",
            "failed",
            "cancelled"
          ],
          "description": "The current status of the turn.\n\n- `queued` - The turn is waiting to start.\n- `in_progress` - The turn is in progress.\n- `waiting` - The turn is waiting for external input.\n- `completed` - The turn completed successfully.\n- `failed` - The turn failed.\n- `cancelled` - The turn was cancelled."
        },
        "subagent_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the subagent that ran the turn, if applicable."
        },
        "usage": {
          "anyOf": [
            {
              "$ref": "#/definitions/TokenUsage"
            },
            {
              "type": "null"
            }
          ],
          "description": "Best-effort token usage for the turn, or null if unknown. Recorded usage may change."
        }
      },
      "required": [
        "id",
        "agent_id",
        "completed_at",
        "created_at",
        "error",
        "object",
        "session_id",
        "started_at",
        "status",
        "subagent_id",
        "usage"
      ],
      "additionalProperties": false,
      "description": "The canonical public representation of a session turn."
    },
    "SessionTurnError": {
      "type": "object",
      "properties": {
        "code": {
          "type": "string",
          "enum": [
            "context_length_exceeded",
            "session_budget_exceeded",
            "usage_limit_exceeded",
            "credit_balance_exhausted",
            "rate_limit_exceeded",
            "server_overloaded",
            "cyber_policy",
            "connection_failed",
            "server_error",
            "authentication_error",
            "invalid_request",
            "resource_not_found",
            "sandbox_error",
            "executor_version_incompatible",
            "active_turn_not_steerable",
            "request_timeout",
            "internal_error"
          ],
          "description": "A stable, machine-readable failure category.\n\n- `context_length_exceeded` - The request exceeds the model's context window.\n- `session_budget_exceeded` - The session has reached its usage budget.\n- `usage_limit_exceeded` - The organization has reached a usage, plan, or   billing limit.\n- `credit_balance_exhausted` - The organization has no API credits remaining.\n- `rate_limit_exceeded` - The request exceeds the available rate limit.\n- `server_overloaded` - The model service is temporarily overloaded.\n- `cyber_policy` - The request was rejected by a safety policy.\n- `connection_failed` - The request could not connect to the model service.\n- `server_error` - The model service encountered an unexpected error.\n- `authentication_error` - The API credentials are invalid or lack the required   access.\n- `invalid_request` - The request contains invalid input or configuration.\n- `resource_not_found` - The requested model or resource is unavailable.\n- `sandbox_error` - The request could not complete in its execution environment.\n- `executor_version_incompatible` - The executor must be upgraded before it can   run this turn.\n- `active_turn_not_steerable` - The session cannot accept additional input while   a request is running.\n- `request_timeout` - The request timed out before the model service responded.\n- `internal_error` - An unexpected internal error prevented the session request   from completing."
        },
        "message": {
          "type": "string",
          "description": "A customer-safe explanation of the failure."
        }
      },
      "required": [
        "code",
        "message"
      ],
      "additionalProperties": false,
      "description": "A customer-safe error describing why a session request failed."
    },
    "AgentSessionTurnInProgressEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn": {
          "$ref": "#/definitions/Turn",
          "description": "The turn at the time it started running."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn associated with the event."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.in_progress",
          "description": "The type of the object. Always `agent.session.turn.in_progress`."
        }
      },
      "required": [
        "event_id",
        "session_id",
        "turn",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a turn starts running."
    },
    "AgentSessionTurnCompletedEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn": {
          "$ref": "#/definitions/Turn",
          "description": "The completed turn."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn associated with the event."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.completed",
          "description": "The type of the object. Always `agent.session.turn.completed`."
        },
        "usage": {
          "anyOf": [
            {
              "$ref": "#/definitions/TokenUsage"
            },
            {
              "type": "null"
            }
          ],
          "description": "Token usage by the root agent during the turn, when available."
        }
      },
      "required": [
        "event_id",
        "session_id",
        "turn",
        "turn_id",
        "type",
        "usage"
      ],
      "additionalProperties": false,
      "description": "Emitted when a turn completes."
    },
    "AgentSessionTurnFailedEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn": {
          "$ref": "#/definitions/Turn",
          "description": "The failed turn."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn associated with the event."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.failed",
          "description": "The type of the object. Always `agent.session.turn.failed`."
        },
        "usage": {
          "anyOf": [
            {
              "$ref": "#/definitions/TokenUsage"
            },
            {
              "type": "null"
            }
          ],
          "description": "Token usage by the root agent during the turn, when available."
        }
      },
      "required": [
        "event_id",
        "session_id",
        "turn",
        "turn_id",
        "type",
        "usage"
      ],
      "additionalProperties": false,
      "description": "Emitted when a turn fails."
    },
    "AgentSessionTurnCancelledEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn": {
          "$ref": "#/definitions/Turn",
          "description": "The cancelled turn."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn associated with the event."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.cancelled",
          "description": "The type of the object. Always `agent.session.turn.cancelled`."
        },
        "usage": {
          "anyOf": [
            {
              "$ref": "#/definitions/TokenUsage"
            },
            {
              "type": "null"
            }
          ],
          "description": "Token usage by the root agent during the turn, when available."
        }
      },
      "required": [
        "event_id",
        "session_id",
        "turn",
        "turn_id",
        "type",
        "usage"
      ],
      "additionalProperties": false,
      "description": "Emitted when a turn is cancelled."
    },
    "AgentSessionTurnItemAddedEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "item": {
          "$ref": "#/definitions/AgentSessionItem",
          "description": "The item that was added."
        },
        "output_index": {
          "type": [
            "number",
            "null"
          ],
          "description": "The index of the item in the turn output, when the item is agent output."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.item.added",
          "description": "The type of the object. Always `agent.session.turn.item.added`."
        }
      },
      "required": [
        "event_id",
        "item",
        "output_index",
        "session_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when an item is added to a turn."
    },
    "AgentSessionItem": {
      "anyOf": [
        {
          "$ref": "#/definitions/AgentSessionMessage"
        },
        {
          "$ref": "#/definitions/AgentReasoningItem"
        },
        {
          "$ref": "#/definitions/AgentFunctionCallItem"
        },
        {
          "$ref": "#/definitions/AgentSessionItem.FunctionCallOutputItemResource"
        },
        {
          "$ref": "#/definitions/AgentSessionItem.AgentMessageItemResource"
        },
        {
          "$ref": "#/definitions/AgentMcpCallItem"
        },
        {
          "$ref": "#/definitions/AgentWebSearchCallItem"
        },
        {
          "$ref": "#/definitions/AgentCommandExecutionItem"
        },
        {
          "$ref": "#/definitions/AgentCreateSubagentCallItem"
        },
        {
          "$ref": "#/definitions/AgentSendSubagentInputCallItem"
        },
        {
          "$ref": "#/definitions/AgentResumeSubagentCallItem"
        },
        {
          "$ref": "#/definitions/AgentWaitForSubagentsCallItem"
        },
        {
          "$ref": "#/definitions/AgentInterruptSubagentCallItem"
        },
        {
          "$ref": "#/definitions/AgentCloseSubagentCallItem"
        }
      ],
      "description": "An item associated with a session turn."
    },
    "AgentSessionMessage": {
      "type": "object",
      "properties": {
        "id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of this item, or null for legacy user messages whose ID was not recorded."
        },
        "content": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/AgentSessionMessageContent"
          },
          "description": "The content of the message. User messages contain input text or images; assistant messages contain output text."
        },
        "phase": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "commentary",
            "final_answer",
            null
          ],
          "description": "The phase of an assistant message. Null for user messages.\n\n- `commentary` - Commentary produced while the agent works.\n- `final_answer` - The agent's final answer."
        },
        "role": {
          "type": "string",
          "enum": [
            "user",
            "assistant"
          ],
          "description": "The role of the message author."
        },
        "status": {
          "$ref": "#/definitions/AgentOutputItemStatus",
          "description": "The status of the message. User messages are always `completed`."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "message",
          "description": "The item type. Always `message`."
        }
      },
      "required": [
        "id",
        "content",
        "phase",
        "role",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A user or assistant message recorded in a session."
    },
    "AgentSessionMessageContent": {
      "anyOf": [
        {
          "$ref": "#/definitions/AgentSessionMessageContent.MessageContentResourceInputText"
        },
        {
          "$ref": "#/definitions/AgentSessionMessageContent.MessageContentResourceInputImage"
        },
        {
          "$ref": "#/definitions/AgentSessionMessageContent.MessageContentResourceOutputText"
        }
      ],
      "description": "A content part in a session message."
    },
    "AgentSessionMessageContent.MessageContentResourceInputText": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The text supplied by the user."
        },
        "type": {
          "type": "string",
          "const": "input_text",
          "description": "The type of the object. Always `input_text`."
        }
      },
      "required": [
        "text",
        "type"
      ],
      "additionalProperties": false,
      "description": "Text supplied by the user."
    },
    "AgentSessionMessageContent.MessageContentResourceInputImage": {
      "type": "object",
      "properties": {
        "image_url": {
          "type": "string",
          "description": "The URL of the image supplied by the user, which may be a base64-encoded data URL."
        },
        "type": {
          "type": "string",
          "const": "input_image",
          "description": "The type of the object. Always `input_image`."
        }
      },
      "required": [
        "image_url",
        "type"
      ],
      "additionalProperties": false,
      "description": "An image supplied by the user."
    },
    "AgentSessionMessageContent.MessageContentResourceOutputText": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The text produced by the assistant."
        },
        "type": {
          "type": "string",
          "const": "output_text",
          "description": "The type of the object. Always `output_text`."
        }
      },
      "required": [
        "text",
        "type"
      ],
      "additionalProperties": false,
      "description": "Text produced by the assistant."
    },
    "AgentOutputItemStatus": {
      "type": "string",
      "enum": [
        "in_progress",
        "completed",
        "incomplete"
      ],
      "description": "The status of an agent output item.\n\n- `in_progress` - The item is in progress.\n- `completed` - The item is complete.\n- `incomplete` - The item stopped before completing."
    },
    "AgentReasoningItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the reasoning item."
        },
        "status": {
          "anyOf": [
            {
              "$ref": "#/definitions/AgentOutputItemStatus"
            },
            {
              "type": "null"
            }
          ],
          "description": "The status of the reasoning item."
        },
        "summary": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/SummaryText"
          },
          "description": "The reasoning summaries produced by the agent."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "reasoning",
          "description": "The item type. Always `reasoning`."
        }
      },
      "required": [
        "id",
        "status",
        "summary",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A reasoning item produced by the agent."
    },
    "SummaryText": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The reasoning summary text."
        },
        "type": {
          "type": "string",
          "const": "summary_text",
          "description": "The content type. Always `summary_text`."
        }
      },
      "required": [
        "text",
        "type"
      ],
      "additionalProperties": false,
      "description": "A reasoning summary content part."
    },
    "AgentFunctionCallItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the function call item."
        },
        "arguments": {
          "description": "The arguments to pass to the function."
        },
        "call_id": {
          "type": "string",
          "description": "The ID used to submit the function result."
        },
        "name": {
          "type": "string",
          "description": "The name of the function to call."
        },
        "status": {
          "$ref": "#/definitions/AgentFunctionCallStatus",
          "description": "The status of the function call."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "function_call",
          "description": "The item type. Always `function_call`."
        }
      },
      "required": [
        "id",
        "arguments",
        "call_id",
        "name",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A function call produced by the agent."
    },
    "AgentFunctionCallStatus": {
      "type": "string",
      "enum": [
        "in_progress",
        "completed",
        "failed",
        "incomplete"
      ],
      "description": "The status of a tool call.\n\n- `in_progress` - The call is in progress.\n- `completed` - The call completed successfully.\n- `failed` - The call failed.\n- `incomplete` - The call stopped before completing."
    },
    "AgentSessionItem.FunctionCallOutputItemResource": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the function call output item."
        },
        "call_id": {
          "type": "string",
          "description": "The ID of the function call that produced this output."
        },
        "error": {
          "type": [
            "string",
            "null"
          ],
          "description": "The error message, if the call failed."
        },
        "output": {
          "anyOf": [
            {
              "$ref": "#/definitions/AgentFunctionCallOutput"
            },
            {
              "type": "null"
            }
          ],
          "description": "The function result, if the call succeeded."
        },
        "status": {
          "$ref": "#/definitions/AgentFunctionCallStatus",
          "description": "The status of the function call."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "function_call_output",
          "description": "The item type. Always `function_call_output`."
        }
      },
      "required": [
        "id",
        "call_id",
        "error",
        "output",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "The result supplied for a function call."
    },
    "AgentFunctionCallOutput": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/InputContent"
          }
        }
      ],
      "description": "The text or model-input content supplied as a function result."
    },
    "InputContent": {
      "anyOf": [
        {
          "$ref": "#/definitions/InputContent.InputContentResourceInputText"
        },
        {
          "$ref": "#/definitions/InputContent.InputContentResourceInputImage"
        }
      ],
      "description": "User-provided content recorded in a session item."
    },
    "InputContent.InputContentResourceInputText": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The text supplied to the agent."
        },
        "type": {
          "type": "string",
          "const": "input_text",
          "description": "The type of the object. Always `input_text`."
        }
      },
      "required": [
        "text",
        "type"
      ],
      "additionalProperties": false,
      "description": "Text input recorded in a session item."
    },
    "InputContent.InputContentResourceInputImage": {
      "type": "object",
      "properties": {
        "image_url": {
          "type": "string",
          "description": "The URL of the image supplied to the agent, which may be a base64-encoded data URL."
        },
        "type": {
          "type": "string",
          "const": "input_image",
          "description": "The type of the object. Always `input_image`."
        }
      },
      "required": [
        "image_url",
        "type"
      ],
      "additionalProperties": false,
      "description": "Image input recorded in a session item."
    },
    "AgentSessionItem.AgentMessageItemResource": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the message."
        },
        "content": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/AgentContent"
          },
          "description": "The content exchanged between the agents."
        },
        "recipient_agent_id": {
          "type": "string",
          "description": "The ID or name of the receiving agent."
        },
        "sender_agent_id": {
          "type": "string",
          "description": "The ID or name of the sending agent."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "agent_message",
          "description": "The item type. Always `agent_message`."
        }
      },
      "required": [
        "id",
        "content",
        "recipient_agent_id",
        "sender_agent_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A message exchanged between agent threads."
    },
    "AgentContent": {
      "anyOf": [
        {
          "$ref": "#/definitions/OutputText"
        },
        {
          "$ref": "#/definitions/AgentContent.EncryptedContentResource"
        }
      ],
      "description": "A plaintext or encrypted content part exchanged between agents."
    },
    "OutputText": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The text produced by the agent."
        },
        "type": {
          "type": "string",
          "const": "output_text",
          "description": "The content type. Always `output_text`."
        }
      },
      "required": [
        "text",
        "type"
      ],
      "additionalProperties": false,
      "description": "A text content part produced by the agent."
    },
    "AgentContent.EncryptedContentResource": {
      "type": "object",
      "properties": {
        "encrypted_content": {
          "type": "string",
          "description": "The encrypted content payload."
        },
        "type": {
          "type": "string",
          "const": "encrypted_content",
          "description": "The content type. Always `encrypted_content`."
        }
      },
      "required": [
        "encrypted_content",
        "type"
      ],
      "additionalProperties": false,
      "description": "Encrypted content exchanged between agents."
    },
    "AgentMcpCallItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the MCP call item."
        },
        "arguments": {
          "description": "The arguments passed to the MCP tool."
        },
        "error": {
          "description": "The error returned by the MCP tool, if any."
        },
        "name": {
          "type": "string",
          "description": "The name of the MCP tool."
        },
        "output": {
          "description": "The output returned by the MCP tool, if any."
        },
        "server_label": {
          "type": "string",
          "description": "The label of the MCP server."
        },
        "status": {
          "$ref": "#/definitions/AgentFunctionCallStatus",
          "description": "The status of the MCP tool call."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "mcp_call",
          "description": "The item type. Always `mcp_call`."
        }
      },
      "required": [
        "id",
        "arguments",
        "error",
        "name",
        "output",
        "server_label",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A call to a tool on an MCP server."
    },
    "AgentWebSearchCallItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the web search call."
        },
        "action": {
          "anyOf": [
            {
              "$ref": "#/definitions/WebSearchAction"
            },
            {
              "type": "null"
            }
          ],
          "description": "The action performed by the web search tool."
        },
        "status": {
          "$ref": "#/definitions/AgentOutputItemStatus",
          "description": "The status of the web search call."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "web_search_call",
          "description": "The item type. Always `web_search_call`."
        }
      },
      "required": [
        "id",
        "action",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A web search call produced by the agent."
    },
    "WebSearchAction": {
      "anyOf": [
        {
          "$ref": "#/definitions/WebSearchAction.WebSearchActionResourceSearch"
        },
        {
          "$ref": "#/definitions/WebSearchAction.WebSearchActionResourceOpenPage"
        },
        {
          "$ref": "#/definitions/WebSearchAction.WebSearchActionResourceFindInPage"
        },
        {
          "$ref": "#/definitions/WebSearchAction.WebSearchActionResourceOther"
        }
      ],
      "description": "An action performed by the web search tool."
    },
    "WebSearchAction.WebSearchActionResourceSearch": {
      "type": "object",
      "properties": {
        "queries": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "The search queries, when multiple queries were used."
        },
        "query": {
          "type": [
            "string",
            "null"
          ],
          "description": "The search query, when a single query was used."
        },
        "type": {
          "type": "string",
          "const": "search",
          "description": "The type of the object. Always `search`."
        }
      },
      "required": [
        "queries",
        "query",
        "type"
      ],
      "additionalProperties": false,
      "description": "A search query or group of search queries."
    },
    "WebSearchAction.WebSearchActionResourceOpenPage": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "open_page",
          "description": "The type of the object. Always `open_page`."
        },
        "url": {
          "type": [
            "string",
            "null"
          ],
          "description": "The URL of the page that was opened."
        }
      },
      "required": [
        "type",
        "url"
      ],
      "additionalProperties": false,
      "description": "Opens a web page."
    },
    "WebSearchAction.WebSearchActionResourceFindInPage": {
      "type": "object",
      "properties": {
        "pattern": {
          "type": [
            "string",
            "null"
          ],
          "description": "The text pattern that was searched for."
        },
        "type": {
          "type": "string",
          "const": "find_in_page",
          "description": "The type of the object. Always `find_in_page`."
        },
        "url": {
          "type": [
            "string",
            "null"
          ],
          "description": "The URL of the page that was searched."
        }
      },
      "required": [
        "pattern",
        "type",
        "url"
      ],
      "additionalProperties": false,
      "description": "Finds text within a web page."
    },
    "WebSearchAction.WebSearchActionResourceOther": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "other",
          "description": "The type of the object. Always `other`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Another web search action."
    },
    "AgentCommandExecutionItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the command execution item."
        },
        "command": {
          "type": "string",
          "description": "The command that was executed."
        },
        "cwd": {
          "type": [
            "string",
            "null"
          ],
          "description": "The working directory used to execute the command."
        },
        "duration_ms": {
          "type": [
            "number",
            "null"
          ],
          "description": "The command duration in milliseconds."
        },
        "exit_code": {
          "type": [
            "number",
            "null"
          ],
          "description": "The process exit code, if the command completed."
        },
        "output": {
          "type": [
            "string",
            "null"
          ],
          "description": "The command output, if available."
        },
        "status": {
          "$ref": "#/definitions/AgentFunctionCallStatus",
          "description": "The status of the command execution."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "command_execution",
          "description": "The item type. Always `command_execution`."
        }
      },
      "required": [
        "id",
        "command",
        "cwd",
        "duration_ms",
        "exit_code",
        "output",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A command execution produced by the agent."
    },
    "AgentCreateSubagentCallItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the tool call item."
        },
        "agent_id": {
          "type": "string",
          "description": "The ID of the agent that requested the subagent."
        },
        "content": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/AgentContent"
          },
          "description": "The task given to the spawned agent."
        },
        "model": {
          "type": [
            "string",
            "null"
          ],
          "description": "The model requested for the spawned agent."
        },
        "reasoning_effort": {
          "type": [
            "string",
            "null"
          ],
          "description": "The reasoning effort requested for the spawned agent."
        },
        "status": {
          "$ref": "#/definitions/AgentFunctionCallStatus",
          "description": "The status of the tool call."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "create_subagent_call",
          "description": "The item type. Always `create_subagent_call`."
        }
      },
      "required": [
        "id",
        "agent_id",
        "content",
        "model",
        "reasoning_effort",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A request to spawn a subagent."
    },
    "AgentSendSubagentInputCallItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the tool call item."
        },
        "content": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/AgentContent"
          },
          "description": "The input sent to the receiving agent."
        },
        "recipient_agent_id": {
          "type": "string",
          "description": "The ID of the agent receiving the input."
        },
        "sender_agent_id": {
          "type": "string",
          "description": "The ID of the agent sending the input."
        },
        "status": {
          "$ref": "#/definitions/AgentFunctionCallStatus",
          "description": "The status of the tool call."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "send_subagent_input_call",
          "description": "The item type. Always `send_subagent_input_call`."
        }
      },
      "required": [
        "id",
        "content",
        "recipient_agent_id",
        "sender_agent_id",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A request to send input to another agent."
    },
    "AgentResumeSubagentCallItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the tool call item."
        },
        "recipient_agent_id": {
          "type": "string",
          "description": "The ID of the agent to resume."
        },
        "sender_agent_id": {
          "type": "string",
          "description": "The ID of the agent requesting the resume."
        },
        "status": {
          "$ref": "#/definitions/AgentFunctionCallStatus",
          "description": "The status of the tool call."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "resume_subagent_call",
          "description": "The item type. Always `resume_subagent_call`."
        }
      },
      "required": [
        "id",
        "recipient_agent_id",
        "sender_agent_id",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A request to resume a subagent."
    },
    "AgentWaitForSubagentsCallItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the tool call item."
        },
        "recipient_agent_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The IDs of the agents to wait for."
        },
        "sender_agent_id": {
          "type": "string",
          "description": "The ID of the agent waiting for results."
        },
        "status": {
          "$ref": "#/definitions/AgentFunctionCallStatus",
          "description": "The status of the tool call."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "wait_for_subagents_call",
          "description": "The item type. Always `wait_for_subagents_call`."
        }
      },
      "required": [
        "id",
        "recipient_agent_ids",
        "sender_agent_id",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A request to wait for one or more subagents."
    },
    "AgentInterruptSubagentCallItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the tool call item."
        },
        "recipient_agent_id": {
          "type": "string",
          "description": "The ID of the agent to interrupt."
        },
        "sender_agent_id": {
          "type": "string",
          "description": "The ID of the agent requesting the interrupt."
        },
        "status": {
          "$ref": "#/definitions/AgentFunctionCallStatus",
          "description": "The status of the tool call."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "interrupt_subagent_call",
          "description": "The item type. Always `interrupt_subagent_call`."
        }
      },
      "required": [
        "id",
        "recipient_agent_id",
        "sender_agent_id",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A request to interrupt a subagent's current turn. The subagent remains available."
    },
    "AgentCloseSubagentCallItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the tool call item."
        },
        "recipient_agent_id": {
          "type": "string",
          "description": "The ID of the agent to close."
        },
        "sender_agent_id": {
          "type": "string",
          "description": "The ID of the agent requesting the close."
        },
        "status": {
          "$ref": "#/definitions/AgentFunctionCallStatus",
          "description": "The status of the tool call."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "close_subagent_call",
          "description": "The item type. Always `close_subagent_call`."
        }
      },
      "required": [
        "id",
        "recipient_agent_id",
        "sender_agent_id",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "A request to close a subagent."
    },
    "AgentSessionIdleEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session": {
          "$ref": "#/definitions/AgentSession",
          "description": "The session that became idle."
        },
        "type": {
          "type": "string",
          "const": "agent.session.idle",
          "description": "The type of the object. Always `agent.session.idle`."
        }
      },
      "required": [
        "event_id",
        "session",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a session becomes idle."
    },
    "AgentSessionInProgressEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session": {
          "$ref": "#/definitions/AgentSession",
          "description": "The session that started processing."
        },
        "type": {
          "type": "string",
          "const": "agent.session.in_progress",
          "description": "The type of the object. Always `agent.session.in_progress`."
        }
      },
      "required": [
        "event_id",
        "session",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a session starts processing a turn."
    },
    "AgentSessionRequiresActionEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session": {
          "$ref": "#/definitions/AgentSession",
          "description": "The session and its current required actions."
        },
        "type": {
          "type": "string",
          "const": "agent.session.requires_action",
          "description": "The type of the object. Always `agent.session.requires_action`."
        }
      },
      "required": [
        "event_id",
        "session",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a session is waiting for one or more required actions."
    },
    "AgentSessionFailedEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session": {
          "$ref": "#/definitions/AgentSession",
          "description": "The failed session."
        },
        "type": {
          "type": "string",
          "const": "agent.session.failed",
          "description": "The type of the object. Always `agent.session.failed`."
        }
      },
      "required": [
        "event_id",
        "session",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a session fails."
    },
    "AgentSessionEnvironmentPendingEvent": {
      "type": "object",
      "properties": {
        "environment": {
          "$ref": "#/definitions/AgentSessionEnvironmentState",
          "description": "The current environment state."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.environment.pending",
          "description": "The type of the object. Always `agent.session.environment.pending`."
        }
      },
      "required": [
        "environment",
        "event_id",
        "session_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted while a session environment is being prepared."
    },
    "AgentSessionEnvironmentConnectedEvent": {
      "type": "object",
      "properties": {
        "environment": {
          "$ref": "#/definitions/AgentSessionEnvironmentState",
          "description": "The current environment state."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.environment.connected",
          "description": "The type of the object. Always `agent.session.environment.connected`."
        }
      },
      "required": [
        "environment",
        "event_id",
        "session_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a session environment connects."
    },
    "AgentSessionEnvironmentDisconnectedEvent": {
      "type": "object",
      "properties": {
        "environment": {
          "$ref": "#/definitions/AgentSessionEnvironmentState",
          "description": "The current environment state."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.environment.disconnected",
          "description": "The type of the object. Always `agent.session.environment.disconnected`."
        }
      },
      "required": [
        "environment",
        "event_id",
        "session_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a session environment disconnects."
    },
    "AgentSessionEnvironmentFailedEvent": {
      "type": "object",
      "properties": {
        "environment": {
          "$ref": "#/definitions/AgentSessionEnvironmentState",
          "description": "The current environment state."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.environment.failed",
          "description": "The type of the object. Always `agent.session.environment.failed`."
        }
      },
      "required": [
        "environment",
        "event_id",
        "session_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a session environment fails."
    },
    "AgentSessionSubagentCreatedEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "subagent": {
          "$ref": "#/definitions/Subagent",
          "description": "The subagent that was created."
        },
        "type": {
          "type": "string",
          "const": "agent.session.subagent.created",
          "description": "The type of the object. Always `agent.session.subagent.created`."
        }
      },
      "required": [
        "event_id",
        "subagent",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a subagent is created."
    },
    "Subagent": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the subagent."
        },
        "closed_at": {
          "type": [
            "number",
            "null"
          ],
          "description": "The Unix timestamp, in seconds, when the subagent was closed. Null while active, including after resume."
        },
        "instructions": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AgentContent"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Initial task content, or null when unavailable. Text may contain placeholders for images or audio when only a preview is available."
        },
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "The runner-assigned nickname, or null when unavailable."
        },
        "object": {
          "type": "string",
          "const": "agent.session.subagent",
          "description": "The object type. Always `agent.session.subagent`."
        },
        "opened_at": {
          "type": "number",
          "description": "The Unix timestamp, in seconds, when the subagent was first opened. Resuming does not change it."
        },
        "parent_agent_id": {
          "type": "string",
          "description": "The ID of the agent that created this subagent."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session that owns the subagent."
        },
        "status": {
          "type": "string",
          "enum": [
            "active",
            "closed"
          ],
          "description": "The current status of the subagent.\n\n- `active` - The subagent remains available, including while idle between turns.\n- `closed` - The subagent is closed."
        }
      },
      "required": [
        "id",
        "closed_at",
        "instructions",
        "name",
        "object",
        "opened_at",
        "parent_agent_id",
        "session_id",
        "status"
      ],
      "additionalProperties": false,
      "description": "A subagent created within a session."
    },
    "AgentSessionSubagentActiveEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "subagent": {
          "$ref": "#/definitions/Subagent",
          "description": "The subagent that resumed."
        },
        "type": {
          "type": "string",
          "const": "agent.session.subagent.active",
          "description": "The type of the object. Always `agent.session.subagent.active`."
        }
      },
      "required": [
        "event_id",
        "subagent",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a closed subagent successfully resumes."
    },
    "AgentSessionSubagentClosedEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "subagent": {
          "$ref": "#/definitions/Subagent",
          "description": "The subagent that was closed."
        },
        "type": {
          "type": "string",
          "const": "agent.session.subagent.closed",
          "description": "The type of the object. Always `agent.session.subagent.closed`."
        }
      },
      "required": [
        "event_id",
        "subagent",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a subagent is closed."
    },
    "AgentSessionTurnItemDoneEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "item": {
          "$ref": "#/definitions/AgentOutputItem",
          "description": "The completed output item."
        },
        "output_index": {
          "type": "number",
          "description": "The index of the output item in the turn output."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.item.done",
          "description": "The type of the object. Always `agent.session.turn.item.done`."
        }
      },
      "required": [
        "event_id",
        "item",
        "output_index",
        "session_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when an output item is complete."
    },
    "AgentOutputItem": {
      "anyOf": [
        {
          "$ref": "#/definitions/AgentSessionAssistantMessage"
        },
        {
          "$ref": "#/definitions/AgentReasoningItem"
        },
        {
          "$ref": "#/definitions/AgentFunctionCallItem"
        },
        {
          "$ref": "#/definitions/AgentMcpCallItem"
        },
        {
          "$ref": "#/definitions/AgentWebSearchCallItem"
        },
        {
          "$ref": "#/definitions/AgentCommandExecutionItem"
        },
        {
          "$ref": "#/definitions/AgentCreateSubagentCallItem"
        },
        {
          "$ref": "#/definitions/AgentSendSubagentInputCallItem"
        },
        {
          "$ref": "#/definitions/AgentResumeSubagentCallItem"
        },
        {
          "$ref": "#/definitions/AgentWaitForSubagentsCallItem"
        },
        {
          "$ref": "#/definitions/AgentInterruptSubagentCallItem"
        },
        {
          "$ref": "#/definitions/AgentCloseSubagentCallItem"
        }
      ],
      "description": "An output item produced by an agent."
    },
    "AgentSessionAssistantMessage": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the message."
        },
        "content": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/OutputText"
          },
          "description": "The content of the message."
        },
        "phase": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "commentary",
            "final_answer",
            null
          ],
          "description": "The phase of the assistant message.\n\n- `commentary` - Commentary produced while the agent works.\n- `final_answer` - The agent's final answer."
        },
        "role": {
          "type": "string",
          "const": "assistant",
          "description": "The role of the message author. Always `assistant`."
        },
        "status": {
          "$ref": "#/definitions/AgentOutputItemStatus",
          "description": "The status of the message."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the turn that contains this item."
        },
        "type": {
          "type": "string",
          "const": "message",
          "description": "The item type. Always `message`."
        }
      },
      "required": [
        "id",
        "content",
        "phase",
        "role",
        "status",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "An assistant message produced by the agent."
    },
    "AgentSessionTurnContentPartAddedEvent": {
      "type": "object",
      "properties": {
        "content_index": {
          "type": "number",
          "description": "The index of the content part in the message."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "item_id": {
          "type": "string",
          "description": "The ID of the message item."
        },
        "output_index": {
          "type": "number",
          "description": "The index of the item in the turn output."
        },
        "part": {
          "$ref": "#/definitions/OutputText",
          "description": "The initial content part."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.content_part.added",
          "description": "The type of the object. Always `agent.session.turn.content_part.added`."
        }
      },
      "required": [
        "content_index",
        "event_id",
        "item_id",
        "output_index",
        "part",
        "session_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when an output text content part is added."
    },
    "AgentSessionTurnContentPartDoneEvent": {
      "type": "object",
      "properties": {
        "content_index": {
          "type": "number",
          "description": "The index of the content part in the message."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "item_id": {
          "type": "string",
          "description": "The ID of the message item."
        },
        "output_index": {
          "type": "number",
          "description": "The index of the item in the turn output."
        },
        "part": {
          "$ref": "#/definitions/OutputText",
          "description": "The completed content part."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.content_part.done",
          "description": "The type of the object. Always `agent.session.turn.content_part.done`."
        }
      },
      "required": [
        "content_index",
        "event_id",
        "item_id",
        "output_index",
        "part",
        "session_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when an output content part is complete."
    },
    "AgentSessionTurnOutputTextDeltaEvent": {
      "type": "object",
      "properties": {
        "content_index": {
          "type": "number",
          "description": "The index of the content part in the message."
        },
        "delta": {
          "type": "string",
          "description": "The text that was appended."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "item_id": {
          "type": "string",
          "description": "The ID of the message item."
        },
        "output_index": {
          "type": "number",
          "description": "The index of the item in the turn output."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.output_text.delta",
          "description": "The type of the object. Always `agent.session.turn.output_text.delta`."
        }
      },
      "required": [
        "content_index",
        "delta",
        "event_id",
        "item_id",
        "output_index",
        "session_id",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when text is appended to an output text content part."
    },
    "AgentSessionTurnOutputTextDoneEvent": {
      "type": "object",
      "properties": {
        "content_index": {
          "type": "number",
          "description": "The index of the content part in the message."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "item_id": {
          "type": "string",
          "description": "The ID of the message item."
        },
        "output_index": {
          "type": "number",
          "description": "The index of the item in the turn output."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "text": {
          "type": "string",
          "description": "The complete output text."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.output_text.done",
          "description": "The type of the object. Always `agent.session.turn.output_text.done`."
        }
      },
      "required": [
        "content_index",
        "event_id",
        "item_id",
        "output_index",
        "session_id",
        "text",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when an output text content part is complete."
    },
    "AgentSessionTurnReasoningSummaryPartAddedEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "item_id": {
          "type": "string",
          "description": "The ID of the reasoning item."
        },
        "output_index": {
          "type": "number",
          "description": "The index of the item in the turn output."
        },
        "part": {
          "$ref": "#/definitions/SummaryText",
          "description": "The initial summary part."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "summary_index": {
          "type": "number",
          "description": "The index of the summary content part."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.reasoning_summary_part.added",
          "description": "The type of the object. Always `agent.session.turn.reasoning_summary_part.added`."
        }
      },
      "required": [
        "event_id",
        "item_id",
        "output_index",
        "part",
        "session_id",
        "summary_index",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a reasoning summary content part is added."
    },
    "AgentSessionTurnReasoningSummaryPartDoneEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "item_id": {
          "type": "string",
          "description": "The ID of the reasoning item."
        },
        "output_index": {
          "type": "number",
          "description": "The index of the item in the turn output."
        },
        "part": {
          "$ref": "#/definitions/SummaryText",
          "description": "The completed summary part."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "status": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "incomplete",
            null
          ],
          "description": "Present as `incomplete` when summary generation was interrupted."
        },
        "summary_index": {
          "type": "number",
          "description": "The index of the summary part."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.reasoning_summary_part.done",
          "description": "The type of the object. Always `agent.session.turn.reasoning_summary_part.done`."
        }
      },
      "required": [
        "event_id",
        "item_id",
        "output_index",
        "part",
        "session_id",
        "status",
        "summary_index",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a reasoning summary part is complete."
    },
    "AgentSessionTurnReasoningSummaryTextDeltaEvent": {
      "type": "object",
      "properties": {
        "delta": {
          "type": "string",
          "description": "The summary text that was appended."
        },
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "item_id": {
          "type": "string",
          "description": "The ID of the reasoning item."
        },
        "output_index": {
          "type": "number",
          "description": "The index of the item in the turn output."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "summary_index": {
          "type": "number",
          "description": "The index of the summary content part."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.reasoning_summary_text.delta",
          "description": "The type of the object. Always `agent.session.turn.reasoning_summary_text.delta`."
        }
      },
      "required": [
        "delta",
        "event_id",
        "item_id",
        "output_index",
        "session_id",
        "summary_index",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when text is appended to a reasoning summary."
    },
    "AgentSessionTurnReasoningSummaryTextDoneEvent": {
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The unique ID of the event."
        },
        "item_id": {
          "type": "string",
          "description": "The ID of the reasoning item."
        },
        "output_index": {
          "type": "number",
          "description": "The index of the item in the turn output."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session associated with the event."
        },
        "summary_index": {
          "type": "number",
          "description": "The index of the summary content part."
        },
        "text": {
          "type": "string",
          "description": "The complete reasoning summary text."
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The ID of the turn associated with the event, when applicable."
        },
        "type": {
          "type": "string",
          "const": "agent.session.turn.reasoning_summary_text.done",
          "description": "The type of the object. Always `agent.session.turn.reasoning_summary_text.done`."
        }
      },
      "required": [
        "event_id",
        "item_id",
        "output_index",
        "session_id",
        "summary_index",
        "text",
        "turn_id",
        "type"
      ],
      "additionalProperties": false,
      "description": "Emitted when a reasoning summary content part is complete."
    },
    "ItemListParams": {
      "type": "object",
      "properties": {
        "after": {
          "type": "string"
        },
        "limit": {
          "type": "number"
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "The order in which resources are returned. Defaults to `desc`.\n\n- `asc` - Returns resources in ascending order.\n- `desc` - Returns resources in descending order."
        }
      },
      "additionalProperties": false
    },
    "TurnListParams": {
      "type": "object",
      "properties": {
        "after": {
          "type": "string"
        },
        "limit": {
          "type": "number"
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "The order in which resources are returned. Defaults to `desc`.\n\n- `asc` - Returns resources in ascending order.\n- `desc` - Returns resources in descending order."
        }
      },
      "additionalProperties": false
    },
    "SubagentListParams": {
      "type": "object",
      "properties": {
        "after": {
          "type": "string"
        },
        "limit": {
          "type": "number"
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "The order in which resources are returned. Defaults to `desc`.\n\n- `asc` - Returns resources in ascending order.\n- `desc` - Returns resources in descending order."
        }
      },
      "additionalProperties": false
    },
    "SessionArtifact": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The immutable artifact ID."
        },
        "created_at": {
          "type": "number",
          "description": "The Unix timestamp, in seconds, when the artifact was published."
        },
        "environment_id": {
          "type": "string",
          "description": "The ID of the environment that produced the artifact."
        },
        "object": {
          "type": "string",
          "const": "agent.session.artifact",
          "description": "The object type. Always `agent.session.artifact`."
        },
        "path": {
          "type": "string",
          "description": "The original absolute file path in the execution environment."
        },
        "session_id": {
          "type": "string",
          "description": "The ID of the session that owns the artifact."
        },
        "size_bytes": {
          "type": "number",
          "description": "The immutable artifact size in bytes."
        },
        "turn_id": {
          "type": "string",
          "description": "The ID of the completed turn that published the artifact."
        }
      },
      "required": [
        "id",
        "created_at",
        "environment_id",
        "object",
        "path",
        "session_id",
        "size_bytes",
        "turn_id"
      ],
      "additionalProperties": false,
      "description": "An immutable file published by a completed hosted session turn."
    },
    "SessionArtifactDeleted": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the deleted session artifact."
        },
        "deleted": {
          "type": "boolean",
          "description": "Whether the session artifact was deleted. Always `true`."
        },
        "object": {
          "type": "string",
          "const": "agent.session.artifact.deleted",
          "description": "The object type. Always `agent.session.artifact.deleted`."
        }
      },
      "required": [
        "id",
        "deleted",
        "object"
      ],
      "additionalProperties": false,
      "description": "Confirmation that an immutable session artifact was deleted."
    },
    "ArtifactListParams": {
      "type": "object",
      "properties": {
        "after": {
          "type": [
            "string",
            "null"
          ],
          "description": "Return artifacts after this immutable artifact ID."
        },
        "environment_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "Restrict the listing to artifacts produced by this environment."
        },
        "limit": {
          "type": [
            "number",
            "null"
          ],
          "description": "The maximum number of artifacts to return, between 1 and 100."
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "Sort by creation time and ID. Defaults to descending.\n\n- `asc` - Returns resources in ascending order.\n- `desc` - Returns resources in descending order."
        }
      },
      "additionalProperties": false
    },
    "EnvironmentInfo": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the environment."
        },
        "files": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HostedEnvironmentFile"
          },
          "description": "Files installed in the environment, without their contents."
        },
        "object": {
          "type": "string",
          "const": "agent.environment",
          "description": "The object type. Always `agent.environment`."
        },
        "plugins": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HostedPlugin"
          },
          "description": "Plugins installed in the environment, without their archive contents."
        },
        "skills": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HostedSkill"
          },
          "description": "Skills installed in the environment, without their archive contents."
        },
        "status": {
          "type": "string",
          "enum": [
            "pending",
            "connected",
            "disconnected",
            "expired",
            "failed"
          ],
          "description": "The current environment connection status."
        },
        "type": {
          "type": "string",
          "enum": [
            "openai_hosted",
            "self_hosted"
          ],
          "description": "Whether the environment is hosted by OpenAI or by the application."
        }
      },
      "required": [
        "id",
        "files",
        "object",
        "plugins",
        "skills",
        "status",
        "type"
      ],
      "additionalProperties": false,
      "description": "Safe metadata for a first-class execution environment."
    },
    "EnvironmentTemplate": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the reusable environment template."
        },
        "capability_directories": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Directories that expose capabilities to the agent."
        },
        "created_at": {
          "type": "number",
          "description": "The Unix timestamp, in seconds, when the template was created."
        },
        "files": {
          "type": "array",
          "items": {
            "anyOf": [
              {
                "$ref": "#/definitions/EnvironmentTemplate.HostedTemplateFileResourceFileID"
              },
              {
                "$ref": "#/definitions/EnvironmentTemplate.HostedTemplateFileResourceInline"
              }
            ]
          },
          "description": "Safe file metadata, excluding contents and session-scoped file IDs."
        },
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "An optional human-readable display name for the template."
        },
        "network": {
          "$ref": "#/definitions/EnvironmentTemplate.Network",
          "description": "Runtime network access for each OpenAI-hosted environment."
        },
        "object": {
          "type": "string",
          "const": "agent.environment.template",
          "description": "The object type. Always `agent.environment.template`."
        },
        "packages": {
          "$ref": "#/definitions/EnvironmentTemplate.Packages",
          "description": "Packages installed in each fresh OpenAI-hosted environment."
        },
        "plugins": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HostedPlugin"
          },
          "description": "Safe plugin metadata, excluding inline archive contents."
        },
        "skills": {
          "type": "array",
          "items": {
            "anyOf": [
              {
                "$ref": "#/definitions/EnvironmentTemplate.HostedTemplateSkillResourceSkillReference"
              },
              {
                "$ref": "#/definitions/EnvironmentTemplate.HostedTemplateSkillResourceInline"
              }
            ]
          },
          "description": "Safe skill metadata, preserving unresolved version selectors."
        },
        "updated_at": {
          "type": "number",
          "description": "The Unix timestamp, in seconds, when the template was last updated."
        }
      },
      "required": [
        "id",
        "capability_directories",
        "created_at",
        "files",
        "name",
        "network",
        "object",
        "packages",
        "plugins",
        "skills",
        "updated_at"
      ],
      "additionalProperties": false,
      "description": "Reusable configuration that provisions a fresh OpenAI-hosted environment for each session."
    },
    "EnvironmentTemplate.HostedTemplateFileResourceFileID": {
      "type": "object",
      "properties": {
        "file_id": {
          "type": "string",
          "description": "The ID of the uploaded file."
        },
        "path": {
          "type": "string",
          "description": "The file's absolute path inside the environment."
        },
        "type": {
          "type": "string",
          "const": "file_id",
          "description": "The type of the object. Always `file_id`."
        }
      },
      "required": [
        "file_id",
        "path",
        "type"
      ],
      "additionalProperties": false,
      "description": "A project-scoped Files API reference resolved separately for each session."
    },
    "EnvironmentTemplate.HostedTemplateFileResourceInline": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "The file's absolute path inside the environment."
        },
        "size_bytes": {
          "type": "number",
          "description": "The decoded size of the inline file in bytes."
        },
        "type": {
          "type": "string",
          "const": "inline",
          "description": "The type of the object. Always `inline`."
        }
      },
      "required": [
        "path",
        "size_bytes",
        "type"
      ],
      "additionalProperties": false,
      "description": "Metadata for confidential inline file contents."
    },
    "EnvironmentTemplate.Network": {
      "type": "object",
      "properties": {
        "access": {
          "type": "string",
          "enum": [
            "enabled",
            "disabled",
            "restricted"
          ],
          "description": "The environment's network access mode.\n\n- `enabled` - Allows unrestricted network access.\n- `disabled` - Disables network access.\n- `restricted` - Allows access only to configured domains."
        },
        "allowed_domains": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Domains the environment may access when network access is restricted."
        }
      },
      "required": [
        "access",
        "allowed_domains"
      ],
      "additionalProperties": false,
      "description": "Runtime network access for each OpenAI-hosted environment."
    },
    "EnvironmentTemplate.Packages": {
      "type": "object",
      "properties": {
        "npm": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "npm packages installed globally in the environment."
        },
        "python": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Python packages installed in the environment."
        },
        "system": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "System packages installed in the environment."
        }
      },
      "required": [
        "npm",
        "python",
        "system"
      ],
      "additionalProperties": false,
      "description": "Packages installed in each fresh OpenAI-hosted environment."
    },
    "EnvironmentTemplate.HostedTemplateSkillResourceSkillReference": {
      "type": "object",
      "properties": {
        "skill_id": {
          "type": "string",
          "description": "The referenced skill ID."
        },
        "type": {
          "type": "string",
          "const": "skill_reference",
          "description": "The type of the object. Always `skill_reference`."
        },
        "version": {
          "type": [
            "string",
            "null"
          ],
          "description": "The requested version selector, including `latest`."
        }
      },
      "required": [
        "skill_id",
        "type",
        "version"
      ],
      "additionalProperties": false,
      "description": "A skill resolved afresh from the Skills API whenever a session starts."
    },
    "EnvironmentTemplate.HostedTemplateSkillResourceInline": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string",
          "description": "The skill description declared in `SKILL.md`."
        },
        "name": {
          "type": "string",
          "description": "The skill name declared in `SKILL.md`."
        },
        "type": {
          "type": "string",
          "const": "inline",
          "description": "The type of the object. Always `inline`."
        }
      },
      "required": [
        "description",
        "name",
        "type"
      ],
      "additionalProperties": false,
      "description": "Safe metadata for an inline skill archive."
    },
    "EnvironmentTemplateDeleted": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the deleted environment template."
        },
        "deleted": {
          "type": "boolean",
          "description": "Whether the environment template was deleted. Always `true`."
        },
        "object": {
          "type": "string",
          "const": "agent.environment.template.deleted",
          "description": "The object type. Always `agent.environment.template.deleted`."
        }
      },
      "required": [
        "id",
        "deleted",
        "object"
      ],
      "additionalProperties": false,
      "description": "A deleted reusable environment template."
    },
    "TemplateCreateParams": {
      "type": "object",
      "properties": {
        "capability_directories": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Directories that contain capabilities exposed to the agent. Defaults to an empty list."
        },
        "env": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Environment variables made available to the agent."
        },
        "files": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HostedEnvironmentFileParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Files available before the agent starts. Defaults to an empty list."
        },
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "An optional human-readable display name for the template."
        },
        "network": {
          "anyOf": [
            {
              "$ref": "#/definitions/TemplateCreateParams.Network"
            },
            {
              "type": "null"
            }
          ],
          "description": "Network access policy for the environment. Defaults to disabled for GA requests and enabled for alpha/beta requests."
        },
        "packages": {
          "anyOf": [
            {
              "$ref": "#/definitions/TemplateCreateParams.Packages"
            },
            {
              "type": "null"
            }
          ],
          "description": "Packages to install in the environment. Defaults to empty package lists."
        },
        "plugins": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HostedPluginParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Plugins provided as inline ZIP archives. Defaults to an empty list."
        },
        "setup_commands": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SetupCommandParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Ordered, confidential setup commands. Command bodies are never returned."
        },
        "skills": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HostedSkillParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Skills referenced by ID or provided as inline ZIP archives. Defaults to an empty list."
        }
      },
      "additionalProperties": false
    },
    "TemplateCreateParams.Network": {
      "type": "object",
      "properties": {
        "access": {
          "type": "string",
          "enum": [
            "enabled",
            "disabled",
            "restricted"
          ],
          "description": "The environment's network access mode.\n\n- `enabled` - Allows unrestricted network access.\n- `disabled` - Disables network access.\n- `restricted` - Allows access only to configured domains."
        },
        "allowed_domains": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Domains the environment may access when network access is restricted."
        }
      },
      "required": [
        "access"
      ],
      "additionalProperties": false,
      "description": "Network access policy for the environment. Defaults to disabled for GA requests and enabled for alpha/beta requests."
    },
    "TemplateCreateParams.Packages": {
      "type": "object",
      "properties": {
        "npm": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "npm packages to install globally. Defaults to an empty list."
        },
        "python": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Python packages to install. Defaults to an empty list."
        },
        "system": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "System packages to install. Defaults to an empty list."
        }
      },
      "additionalProperties": false,
      "description": "Packages to install in the environment. Defaults to empty package lists."
    },
    "TemplateUpdateParams": {
      "type": "object",
      "properties": {
        "capability_directories": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Directories that expose capabilities to the agent."
        },
        "env": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Replacement confidential environment values."
        },
        "files": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HostedEnvironmentFileParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Replacement file configuration materialized for each new session."
        },
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "A replacement human-readable display name, or `null` to clear the name."
        },
        "network": {
          "anyOf": [
            {
              "$ref": "#/definitions/TemplateUpdateParams.Network"
            },
            {
              "type": "null"
            }
          ],
          "description": "Network access available after setup completes. Omit to preserve the current policy, or pass `null` to reset to disabled for GA requests or enabled for alpha/beta requests."
        },
        "packages": {
          "anyOf": [
            {
              "$ref": "#/definitions/TemplateUpdateParams.Packages"
            },
            {
              "type": "null"
            }
          ],
          "description": "Packages installed before the runtime network policy applies."
        },
        "plugins": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HostedPluginParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Replacement plugin configuration installed for each new session."
        },
        "setup_commands": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SetupCommandParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Replacement confidential setup commands, never included in returned resources."
        },
        "skills": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HostedSkillParam"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Replacement skill configuration installed for each new session."
        }
      },
      "additionalProperties": false
    },
    "TemplateUpdateParams.Network": {
      "type": "object",
      "properties": {
        "access": {
          "type": "string",
          "enum": [
            "enabled",
            "disabled",
            "restricted"
          ],
          "description": "The environment's network access mode.\n\n- `enabled` - Allows unrestricted network access.\n- `disabled` - Disables network access.\n- `restricted` - Allows access only to configured domains."
        },
        "allowed_domains": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Domains the environment may access when network access is restricted."
        }
      },
      "required": [
        "access"
      ],
      "additionalProperties": false,
      "description": "Network access available after setup completes. Omit to preserve the current policy, or pass `null` to reset to disabled for GA requests or enabled for alpha/beta requests."
    },
    "TemplateUpdateParams.Packages": {
      "type": "object",
      "properties": {
        "npm": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "npm packages to install globally. Defaults to an empty list."
        },
        "python": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Python packages to install. Defaults to an empty list."
        },
        "system": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "System packages to install. Defaults to an empty list."
        }
      },
      "additionalProperties": false,
      "description": "Packages installed before the runtime network policy applies."
    },
    "TemplateListParams": {
      "type": "object",
      "properties": {
        "after": {
          "type": "string"
        },
        "limit": {
          "type": "number"
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "The order in which resources are returned. Defaults to `desc`.\n\n- `asc` - Returns resources in ascending order.\n- `desc` - Returns resources in descending order."
        }
      },
      "additionalProperties": false
    },
    "EnvironmentFile": {
      "type": "object",
      "properties": {
        "environment_id": {
          "type": "string",
          "description": "The ID of the environment containing this file."
        },
        "object": {
          "type": "string",
          "const": "agent.environment.file",
          "description": "The object type. Always `agent.environment.file`."
        },
        "path": {
          "type": "string",
          "description": "The absolute file path inside the environment's workspace."
        },
        "size_bytes": {
          "type": "number",
          "description": "The file size in bytes."
        }
      },
      "required": [
        "environment_id",
        "object",
        "path",
        "size_bytes"
      ],
      "additionalProperties": false,
      "description": "A live file in an execution environment."
    },
    "FileCreateParams": {
      "anyOf": [
        {
          "$ref": "#/definitions/FileCreateParams.HostedEnvironmentFileParamFileID"
        },
        {
          "$ref": "#/definitions/FileCreateParams.HostedEnvironmentFileParamInline"
        }
      ]
    },
    "FileCreateParams.HostedEnvironmentFileParamFileID": {
      "type": "object",
      "properties": {
        "file_id": {
          "type": "string",
          "description": "The ID of the uploaded file."
        },
        "path": {
          "type": "string",
          "description": "The absolute destination path inside `/workspace`."
        },
        "type": {
          "type": "string",
          "const": "file_id",
          "description": "The type of the object. Always `file_id`."
        }
      },
      "required": [
        "file_id",
        "path",
        "type"
      ],
      "additionalProperties": false
    },
    "FileCreateParams.HostedEnvironmentFileParamInline": {
      "type": "object",
      "properties": {
        "data": {
          "type": "string",
          "description": "The standard-base64-encoded file contents."
        },
        "path": {
          "type": "string",
          "description": "The absolute destination path inside `/workspace`."
        },
        "type": {
          "type": "string",
          "const": "inline",
          "description": "The type of the object. Always `inline`."
        }
      },
      "required": [
        "data",
        "path",
        "type"
      ],
      "additionalProperties": false
    },
    "FileListParams": {
      "type": "object",
      "properties": {
        "page": {
          "type": "string"
        },
        "limit": {
          "type": [
            "number",
            "null"
          ],
          "description": "The maximum number of files to return, between 1 and 100."
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "Sort by case-sensitive path components. Defaults to descending.\n\n- `asc` - Returns resources in ascending order.\n- `desc` - Returns resources in descending order."
        },
        "path": {
          "type": [
            "string",
            "null"
          ],
          "description": "Restrict the listing to this absolute workspace directory."
        }
      },
      "additionalProperties": false
    },
    "Vault": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the vault."
        },
        "created_at": {
          "type": "number",
          "description": "The Unix timestamp, in seconds, when the vault was created."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value pairs associated with the vault, such as an application or team identifier."
        },
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "The human-readable name of the vault, if set."
        },
        "object": {
          "type": "string",
          "const": "vault",
          "description": "The object type. Always `vault`."
        }
      },
      "required": [
        "id",
        "created_at",
        "metadata",
        "name",
        "object"
      ],
      "additionalProperties": false,
      "description": "A collection of credentials for MCP servers and OpenAI-hosted environments."
    },
    "VaultDeleted": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the deleted vault."
        },
        "deleted": {
          "type": "boolean",
          "description": "Whether the resource was deleted. Always `true`."
        },
        "object": {
          "type": "string",
          "const": "vault.deleted",
          "description": "The object type. Always `vault.deleted`."
        }
      },
      "required": [
        "id",
        "deleted",
        "object"
      ],
      "additionalProperties": false,
      "description": "Confirmation that a vault was deleted."
    },
    "VaultCreateParams": {
      "type": "object",
      "properties": {
        "metadata": {
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "Key-value pairs to associate with the vault, such as an application or team identifier."
        },
        "name": {
          "type": "string",
          "description": "The name is trimmed before storage. It must contain 1 to 256 UTF-8 bytes after trimming."
        }
      },
      "additionalProperties": false
    },
    "VaultListParams": {
      "type": "object",
      "properties": {
        "after": {
          "type": "string"
        },
        "limit": {
          "type": [
            "number",
            "null"
          ],
          "description": "The maximum number of resources to return. Defaults to 20. Values are clamped between 1 and 100."
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "Sort order by the `created_at` timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `desc`.\n\n- `asc` - Returns resources in ascending order.\n- `desc` - Returns resources in descending order."
        },
        "status": {
          "$ref": "#/definitions/VaultStatusFilter",
          "description": "Filter by one status or a list, such as `status=active` or `status[]=active&status[]=archived`. Both statuses are included by default."
        }
      },
      "additionalProperties": false
    },
    "VaultStatusFilter": {
      "anyOf": [
        {
          "$ref": "#/definitions/VaultStatus"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/VaultStatus"
          }
        }
      ],
      "description": "One or more lifecycle statuses to include when listing vaults or credentials."
    },
    "VaultStatus": {
      "type": "string",
      "enum": [
        "active",
        "archived"
      ],
      "description": "Whether a vault or credential is active or archived."
    },
    "Credential": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the credential."
        },
        "auth": {
          "$ref": "#/definitions/CredentialAuth",
          "description": "The authentication method and non-secret configuration of the credential."
        },
        "created_at": {
          "type": "number",
          "description": "The Unix timestamp, in seconds, when the credential was created."
        },
        "name": {
          "type": "string",
          "description": "The human-readable name of the credential."
        },
        "object": {
          "type": "string",
          "const": "vault.credential",
          "description": "The object type. Always `vault.credential`."
        },
        "updated_at": {
          "type": "number",
          "description": "The Unix timestamp, in seconds, when the credential was last updated."
        },
        "vault_id": {
          "type": "string",
          "description": "The ID of the vault containing this credential."
        }
      },
      "required": [
        "id",
        "auth",
        "created_at",
        "name",
        "object",
        "updated_at",
        "vault_id"
      ],
      "additionalProperties": false,
      "description": "Metadata for a stored credential. Secret values are never returned."
    },
    "CredentialAuth": {
      "anyOf": [
        {
          "$ref": "#/definitions/CredentialAuth.VaultCredentialAuthResourceMcpOauth"
        },
        {
          "$ref": "#/definitions/CredentialAuth.VaultCredentialAuthResourceStaticBearer"
        },
        {
          "$ref": "#/definitions/CredentialAuth.VaultCredentialAuthResourceEnvironmentVariable"
        }
      ],
      "description": "The authentication configuration of a vault credential, excluding secrets."
    },
    "CredentialAuth.VaultCredentialAuthResourceMcpOauth": {
      "type": "object",
      "properties": {
        "expires_at": {
          "type": [
            "string",
            "null"
          ],
          "description": "When the OAuth access token expires, as an RFC 3339 timestamp, if known."
        },
        "mcp_server_url": {
          "type": "string",
          "description": "The HTTPS MCP server URL authorized by this credential."
        },
        "refresh": {
          "anyOf": [
            {
              "$ref": "#/definitions/CredentialAuth.VaultCredentialAuthResourceMcpOauth.Refresh"
            },
            {
              "type": "null"
            }
          ],
          "description": "Public refresh metadata without refresh tokens or OAuth client secrets."
        },
        "type": {
          "type": "string",
          "const": "mcp_oauth",
          "description": "The type of the object. Always `mcp_oauth`."
        }
      },
      "required": [
        "expires_at",
        "mcp_server_url",
        "refresh",
        "type"
      ],
      "additionalProperties": false,
      "description": "Public metadata for an OAuth credential; tokens and client secrets are never returned."
    },
    "CredentialAuth.VaultCredentialAuthResourceMcpOauth.Refresh": {
      "type": "object",
      "properties": {
        "client_id": {
          "type": "string",
          "description": "The OAuth client ID used when requesting a new access token."
        },
        "resource": {
          "type": [
            "string",
            "null"
          ],
          "description": "The resource URI sent to the OAuth token endpoint during refresh, if configured."
        },
        "scope": {
          "type": [
            "string",
            "null"
          ],
          "description": "Space-separated OAuth scopes requested during refresh, if configured."
        },
        "token_endpoint": {
          "type": "string",
          "description": "The HTTPS OAuth token endpoint used for refresh."
        },
        "token_endpoint_auth": {
          "$ref": "#/definitions/McpOauthTokenEndpointAuth",
          "description": "How the OAuth client authenticates to the token endpoint, excluding its client secret."
        }
      },
      "required": [
        "client_id",
        "resource",
        "scope",
        "token_endpoint",
        "token_endpoint_auth"
      ],
      "additionalProperties": false,
      "description": "Public refresh metadata without refresh tokens or OAuth client secrets."
    },
    "McpOauthTokenEndpointAuth": {
      "anyOf": [
        {
          "$ref": "#/definitions/McpOauthTokenEndpointAuth.McpOauthTokenEndpointAuthResourceNone"
        },
        {
          "$ref": "#/definitions/McpOauthTokenEndpointAuth.McpOauthTokenEndpointAuthResourceClientSecretBasic"
        },
        {
          "$ref": "#/definitions/McpOauthTokenEndpointAuth.McpOauthTokenEndpointAuthResourceClientSecretPost"
        }
      ],
      "description": "The client authentication method used for OAuth token refresh."
    },
    "McpOauthTokenEndpointAuth.McpOauthTokenEndpointAuthResourceNone": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "none",
          "description": "The type of the object. Always `none`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Sends the client ID without a client secret."
    },
    "McpOauthTokenEndpointAuth.McpOauthTokenEndpointAuthResourceClientSecretBasic": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "client_secret_basic",
          "description": "The type of the object. Always `client_secret_basic`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Sends the client ID and secret using HTTP Basic authentication."
    },
    "McpOauthTokenEndpointAuth.McpOauthTokenEndpointAuthResourceClientSecretPost": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "client_secret_post",
          "description": "The type of the object. Always `client_secret_post`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Sends the client ID and secret in the token request body."
    },
    "CredentialAuth.VaultCredentialAuthResourceStaticBearer": {
      "type": "object",
      "properties": {
        "mcp_server_url": {
          "type": "string",
          "description": "The HTTPS MCP server URL authorized by this credential."
        },
        "type": {
          "type": "string",
          "const": "static_bearer",
          "description": "The type of the object. Always `static_bearer`."
        }
      },
      "required": [
        "mcp_server_url",
        "type"
      ],
      "additionalProperties": false,
      "description": "Metadata for a bearer-token credential, without automatic OAuth refresh."
    },
    "CredentialAuth.VaultCredentialAuthResourceEnvironmentVariable": {
      "type": "object",
      "properties": {
        "networking": {
          "$ref": "#/definitions/CredentialNetworking",
          "description": "The destinations where the proxy can substitute the secret, subject to the environment network policy."
        },
        "secret_name": {
          "type": "string",
          "description": "The environment variable name that receives the placeholder in the sandbox."
        },
        "type": {
          "type": "string",
          "const": "environment_variable",
          "description": "The type of the object. Always `environment_variable`."
        }
      },
      "required": [
        "networking",
        "secret_name",
        "type"
      ],
      "additionalProperties": false,
      "description": "Metadata for an HTTP credential used only in OpenAI-hosted environments. Sandbox code receives a placeholder. The proxy substitutes the secret for allowed HTTPS destinations on ports 443 and 8443. The real secret is not available to sandbox code for local computation and is never returned in this resource."
    },
    "CredentialNetworking": {
      "anyOf": [
        {
          "$ref": "#/definitions/CredentialNetworking.VaultCredentialNetworkingResourceUnrestricted"
        },
        {
          "$ref": "#/definitions/CredentialNetworking.VaultCredentialNetworkingResourceLimited"
        }
      ],
      "description": "Destination permissions for an environment-variable credential. These do not grant network access to the environment."
    },
    "CredentialNetworking.VaultCredentialNetworkingResourceUnrestricted": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "unrestricted",
          "description": "The type of the object. Always `unrestricted`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Allows substitution for destinations permitted by the environment network policy. Requires `environment.network.access` to be `restricted`, with explicit `allowed_domains`."
    },
    "CredentialNetworking.VaultCredentialNetworkingResourceLimited": {
      "type": "object",
      "properties": {
        "allowed_hosts": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The 1 to 16 distinct allowed hostnames or IPv4 addresses, normalized to lowercase. Entries contain no scheme, path, port, or wildcard. IPv6 addresses are not supported."
        },
        "type": {
          "type": "string",
          "const": "limited",
          "description": "The type of the object. Always `limited`."
        }
      },
      "required": [
        "allowed_hosts",
        "type"
      ],
      "additionalProperties": false,
      "description": "Allows substitution only for the listed hosts. The environment network policy must also allow these hosts."
    },
    "CredentialDeleted": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the deleted credential."
        },
        "deleted": {
          "type": "boolean",
          "description": "Whether the resource was deleted. Always `true`."
        },
        "object": {
          "type": "string",
          "const": "vault.credential.deleted",
          "description": "The object type. Always `vault.credential.deleted`."
        }
      },
      "required": [
        "id",
        "deleted",
        "object"
      ],
      "additionalProperties": false,
      "description": "Confirmation that a vault credential was deleted."
    },
    "CredentialCreateParams": {
      "type": "object",
      "properties": {
        "auth": {
          "$ref": "#/definitions/CredentialAuthCreateParam",
          "description": "The authentication method and write-only secret values to store."
        },
        "name": {
          "type": "string",
          "description": "The name is trimmed before storage. It must contain 1 to 256 UTF-8 bytes after trimming."
        }
      },
      "required": [
        "auth",
        "name"
      ],
      "additionalProperties": false
    },
    "CredentialAuthCreateParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/CredentialAuthCreateParam.CreateVaultCredentialAuthParamMcpOauth"
        },
        {
          "$ref": "#/definitions/CredentialAuthCreateParam.CreateVaultCredentialAuthParamStaticBearer"
        },
        {
          "$ref": "#/definitions/CredentialAuthCreateParam.CreateVaultCredentialAuthParamEnvironmentVariable"
        }
      ],
      "description": "Authentication credentials for an MCP server or an OpenAI-hosted environment."
    },
    "CredentialAuthCreateParam.CreateVaultCredentialAuthParamMcpOauth": {
      "type": "object",
      "properties": {
        "access_token": {
          "type": "string",
          "description": "A write-only OAuth access token; never returned by credential resources."
        },
        "mcp_server_url": {
          "type": "string",
          "description": "The HTTPS MCP server URL authorized by this credential."
        },
        "type": {
          "type": "string",
          "const": "mcp_oauth",
          "description": "The type of the object. Always `mcp_oauth`."
        },
        "expires_at": {
          "type": [
            "string",
            "null"
          ],
          "description": "When the OAuth access token expires, as an RFC 3339 timestamp, if known."
        },
        "refresh": {
          "anyOf": [
            {
              "$ref": "#/definitions/CredentialAuthCreateParam.CreateVaultCredentialAuthParamMcpOauth.Refresh"
            },
            {
              "type": "null"
            }
          ],
          "description": "Optional refresh configuration for an HTTPS OAuth token endpoint."
        }
      },
      "required": [
        "access_token",
        "mcp_server_url",
        "type"
      ],
      "additionalProperties": false,
      "description": "An OAuth credential for an HTTPS MCP destination."
    },
    "CredentialAuthCreateParam.CreateVaultCredentialAuthParamMcpOauth.Refresh": {
      "type": "object",
      "properties": {
        "client_id": {
          "type": "string",
          "description": "The OAuth client ID used when requesting a new access token."
        },
        "refresh_token": {
          "type": "string",
          "description": "The refresh token to store. This secret is never returned in credential resources."
        },
        "token_endpoint": {
          "type": "string",
          "description": "The HTTPS OAuth token endpoint used to exchange the refresh token for a new access token."
        },
        "token_endpoint_auth": {
          "$ref": "#/definitions/McpOauthTokenEndpointAuthCreateParam",
          "description": "How the OAuth client authenticates to the token endpoint."
        },
        "resource": {
          "type": [
            "string",
            "null"
          ],
          "description": "The resource URI to send to the OAuth token endpoint during refresh, if required."
        },
        "scope": {
          "type": [
            "string",
            "null"
          ],
          "description": "Space-separated OAuth scopes to request during refresh, if required."
        }
      },
      "required": [
        "client_id",
        "refresh_token",
        "token_endpoint",
        "token_endpoint_auth"
      ],
      "additionalProperties": false,
      "description": "Optional refresh configuration for an HTTPS OAuth token endpoint."
    },
    "McpOauthTokenEndpointAuthCreateParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/McpOauthTokenEndpointAuthCreateParam.CreateMcpOauthTokenEndpointAuthParamNone"
        },
        {
          "$ref": "#/definitions/McpOauthTokenEndpointAuthCreateParam.CreateMcpOauthTokenEndpointAuthParamClientSecretBasic"
        },
        {
          "$ref": "#/definitions/McpOauthTokenEndpointAuthCreateParam.CreateMcpOauthTokenEndpointAuthParamClientSecretPost"
        }
      ],
      "description": "Client authentication credentials for OAuth token refresh."
    },
    "McpOauthTokenEndpointAuthCreateParam.CreateMcpOauthTokenEndpointAuthParamNone": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "none",
          "description": "The type of the object. Always `none`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Sends the client ID without a client secret."
    },
    "McpOauthTokenEndpointAuthCreateParam.CreateMcpOauthTokenEndpointAuthParamClientSecretBasic": {
      "type": "object",
      "properties": {
        "client_secret": {
          "type": "string",
          "description": "The OAuth client secret to store. Never returned in credential resources."
        },
        "type": {
          "type": "string",
          "const": "client_secret_basic",
          "description": "The type of the object. Always `client_secret_basic`."
        }
      },
      "required": [
        "client_secret",
        "type"
      ],
      "additionalProperties": false,
      "description": "Sends the client ID and secret using HTTP Basic authentication."
    },
    "McpOauthTokenEndpointAuthCreateParam.CreateMcpOauthTokenEndpointAuthParamClientSecretPost": {
      "type": "object",
      "properties": {
        "client_secret": {
          "type": "string",
          "description": "The OAuth client secret to store. Never returned in credential resources."
        },
        "type": {
          "type": "string",
          "const": "client_secret_post",
          "description": "The type of the object. Always `client_secret_post`."
        }
      },
      "required": [
        "client_secret",
        "type"
      ],
      "additionalProperties": false,
      "description": "Sends the client ID and secret in the token request body."
    },
    "CredentialAuthCreateParam.CreateVaultCredentialAuthParamStaticBearer": {
      "type": "object",
      "properties": {
        "token": {
          "type": "string",
          "description": "The bearer token to store. This secret is never returned in credential resources."
        },
        "mcp_server_url": {
          "type": "string",
          "description": "The HTTPS MCP server URL authorized by this credential."
        },
        "type": {
          "type": "string",
          "const": "static_bearer",
          "description": "The type of the object. Always `static_bearer`."
        }
      },
      "required": [
        "token",
        "mcp_server_url",
        "type"
      ],
      "additionalProperties": false,
      "description": "A bearer token for an MCP server, without automatic OAuth refresh."
    },
    "CredentialAuthCreateParam.CreateVaultCredentialAuthParamEnvironmentVariable": {
      "type": "object",
      "properties": {
        "networking": {
          "$ref": "#/definitions/CredentialNetworkingParam",
          "description": "The destinations where the proxy can substitute this secret. The environment network policy must also allow them."
        },
        "secret_name": {
          "type": "string",
          "description": "The environment variable name that receives the placeholder, such as `SERVICE_API_KEY`. Use ASCII letters, digits, and underscores, starting with a letter or underscore. Names starting with `CODEX_` and managed proxy or certificate variable names are reserved."
        },
        "secret_value": {
          "type": "string",
          "description": "The write-only secret to store. Never returned in credential resources or supplied directly to sandbox code. Must be nonempty and must not contain carriage returns, newlines, or NUL bytes."
        },
        "type": {
          "type": "string",
          "const": "environment_variable",
          "description": "The type of the object. Always `environment_variable`."
        }
      },
      "required": [
        "networking",
        "secret_name",
        "secret_value",
        "type"
      ],
      "additionalProperties": false,
      "description": "An HTTP credential for OpenAI-hosted environments only. The sandbox receives an environment variable containing a placeholder, not the secret. Use the placeholder unchanged in outgoing requests. The egress proxy replaces the placeholder with the secret for allowed HTTPS destinations on ports 443 and 8443. Sandbox code cannot read the real secret or use it for local computation, such as signing a request."
    },
    "CredentialNetworkingParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/CredentialNetworkingParam.VaultCredentialNetworkingParamUnrestricted"
        },
        {
          "$ref": "#/definitions/CredentialNetworkingParam.VaultCredentialNetworkingParamLimited"
        }
      ],
      "description": "Destination permissions for an environment-variable credential. These do not grant network access to the environment."
    },
    "CredentialNetworkingParam.VaultCredentialNetworkingParamUnrestricted": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "unrestricted",
          "description": "The type of the object. Always `unrestricted`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Allows substitution for destinations permitted by the environment network policy. Requires `environment.network.access` to be `restricted`, with explicit `allowed_domains`."
    },
    "CredentialNetworkingParam.VaultCredentialNetworkingParamLimited": {
      "type": "object",
      "properties": {
        "allowed_hosts": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The 1 to 16 distinct allowed hostnames or IPv4 addresses, normalized to lowercase. Entries contain no scheme, path, port, or wildcard. IPv6 addresses are not supported."
        },
        "type": {
          "type": "string",
          "const": "limited",
          "description": "The type of the object. Always `limited`."
        }
      },
      "required": [
        "allowed_hosts",
        "type"
      ],
      "additionalProperties": false,
      "description": "Allows substitution only for the listed hosts. The environment network policy must also allow these hosts."
    },
    "CredentialRotateRequest": {
      "type": "object",
      "properties": {
        "auth": {
          "$ref": "#/definitions/CredentialAuthRotateParam",
          "description": "Body param: Replacement values for the credential's existing authentication method."
        }
      },
      "required": [
        "auth"
      ],
      "additionalProperties": false
    },
    "CredentialAuthRotateParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/CredentialAuthRotateParam.RotateVaultCredentialAuthParamMcpOauth"
        },
        {
          "$ref": "#/definitions/CredentialAuthRotateParam.RotateVaultCredentialAuthParamStaticBearer"
        },
        {
          "$ref": "#/definitions/CredentialAuthRotateParam.RotateVaultCredentialAuthParamEnvironmentVariable"
        }
      ],
      "description": "Updates to a vault credential without changing its authentication method or destination configuration."
    },
    "CredentialAuthRotateParam.RotateVaultCredentialAuthParamMcpOauth": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "mcp_oauth",
          "description": "The type of the object. Always `mcp_oauth`."
        },
        "access_token": {
          "type": [
            "string",
            "null"
          ],
          "description": "A write-only replacement OAuth access token."
        },
        "expires_at": {
          "type": [
            "string",
            "null"
          ],
          "description": "The replacement expiry as an RFC 3339 timestamp, or `null` to clear it. Omitting this field preserves the expiry unless a new access token is supplied, in which case the expiry is cleared."
        },
        "refresh": {
          "anyOf": [
            {
              "$ref": "#/definitions/CredentialAuthRotateParam.RotateVaultCredentialAuthParamMcpOauth.Refresh"
            },
            {
              "type": "null"
            }
          ],
          "description": "Optional write-only refresh-token and client-secret updates."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Rotate an OAuth credential for an HTTPS MCP destination."
    },
    "CredentialAuthRotateParam.RotateVaultCredentialAuthParamMcpOauth.Refresh": {
      "type": "object",
      "properties": {
        "refresh_token": {
          "type": [
            "string",
            "null"
          ],
          "description": "The replacement refresh token. Omit or pass `null` to keep the stored token. This secret is never returned in resources."
        },
        "scope": {
          "type": [
            "string",
            "null"
          ],
          "description": "Replacement space-separated OAuth scopes for refresh requests. Omit to keep the scopes, or pass `null` to stop sending a scope parameter."
        },
        "token_endpoint_auth": {
          "anyOf": [
            {
              "$ref": "#/definitions/McpOauthTokenEndpointAuthRotateParam"
            },
            {
              "type": "null"
            }
          ],
          "description": "Client-secret updates for the existing token endpoint authentication method."
        }
      },
      "additionalProperties": false,
      "description": "Optional write-only refresh-token and client-secret updates."
    },
    "McpOauthTokenEndpointAuthRotateParam": {
      "anyOf": [
        {
          "$ref": "#/definitions/McpOauthTokenEndpointAuthRotateParam.RotateMcpOauthTokenEndpointAuthParamClientSecretBasic"
        },
        {
          "$ref": "#/definitions/McpOauthTokenEndpointAuthRotateParam.RotateMcpOauthTokenEndpointAuthParamClientSecretPost"
        }
      ],
      "description": "Client-secret updates that preserve the credential's OAuth authentication method."
    },
    "McpOauthTokenEndpointAuthRotateParam.RotateMcpOauthTokenEndpointAuthParamClientSecretBasic": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "client_secret_basic",
          "description": "The type of the object. Always `client_secret_basic`."
        },
        "client_secret": {
          "type": [
            "string",
            "null"
          ],
          "description": "The replacement OAuth client secret. Omit or pass `null` to keep the stored secret. This secret is never returned in resources."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Updates credentials sent using HTTP Basic authentication."
    },
    "McpOauthTokenEndpointAuthRotateParam.RotateMcpOauthTokenEndpointAuthParamClientSecretPost": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "client_secret_post",
          "description": "The type of the object. Always `client_secret_post`."
        },
        "client_secret": {
          "type": [
            "string",
            "null"
          ],
          "description": "The replacement OAuth client secret. Omit or pass `null` to keep the stored secret. This secret is never returned in resources."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Updates credentials sent in the token request body."
    },
    "CredentialAuthRotateParam.RotateVaultCredentialAuthParamStaticBearer": {
      "type": "object",
      "properties": {
        "token": {
          "type": "string",
          "description": "The replacement bearer token. This secret is never returned in credential resources."
        },
        "type": {
          "type": "string",
          "const": "static_bearer",
          "description": "The type of the object. Always `static_bearer`."
        }
      },
      "required": [
        "token",
        "type"
      ],
      "additionalProperties": false,
      "description": "Replace the bearer token for the credential's MCP server."
    },
    "CredentialAuthRotateParam.RotateVaultCredentialAuthParamEnvironmentVariable": {
      "type": "object",
      "properties": {
        "secret_value": {
          "type": "string",
          "description": "The write-only replacement secret. Never returned in credential resources or supplied directly to sandbox code. Must be nonempty and must not contain carriage returns, newlines, or NUL bytes."
        },
        "type": {
          "type": "string",
          "const": "environment_variable",
          "description": "The type of the object. Always `environment_variable`."
        }
      },
      "required": [
        "secret_value",
        "type"
      ],
      "additionalProperties": false,
      "description": "Replace the secret for an OpenAI-hosted environment credential. The environment variable name and networking configuration remain unchanged."
    },
    "CredentialListParams": {
      "type": "object",
      "properties": {
        "after": {
          "type": "string"
        },
        "limit": {
          "type": [
            "number",
            "null"
          ],
          "description": "The maximum number of resources to return. Defaults to 20. Values are clamped between 1 and 100."
        },
        "order": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "description": "Sort order by the `created_at` timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `desc`.\n\n- `asc` - Returns resources in ascending order.\n- `desc` - Returns resources in descending order."
        },
        "status": {
          "$ref": "#/definitions/VaultStatusFilter",
          "description": "Filter by one status or a list, such as `status=active` or `status[]=active&status[]=archived`. Both statuses are included by default."
        }
      },
      "additionalProperties": false
    }
  },
  "$id": "agents-api.schema.json",
  "$comment": "Generated from openai@7.23.0 by scripts/generate-agents-api-schema.mjs. Do not edit."
}
